# 지도 개편 v1 작업 메모

## 범위

이번 단계는 기존 하드코딩 ASCII 전체지도를 제거 가능한 구조로 바꾸는 1차 작업이다.

- `DB.maps.connected`를 세계 그래프 정본으로 사용
- `source/world-map.js`에서 SVG 세계지도 렌더링
- 주요 경로는 고정 좌표, 던전·세부지역은 선택 표시
- 현재 위치 / 선택 지역 / 경로 강조
- 선택 지역이 현재 맵의 `connected`에 있으면 기존 `tryMove()`로 이동
- 모바일에서는 지도 캔버스를 가로 스크롤 가능하게 유지
- 최종 배포물은 기존과 동일하게 단일 HTML

## 빌드 구조

`build.py`

1. `source/data/*.json` 로드 및 JSON 검증
2. `db-maps.json` 그래프 감사
3. 기존 데이터 마커 치환
4. `source/world-map.js`를 `</body>` 직전에 인라인
5. `룬미드가츠_v9.19.html`, `index.html` 생성

기존 `source/template.html` 내부의 `showWorldMapModal()`은 아직 남아 있으나, 마지막에 주입된 `world-map.js`가 `window.showWorldMapModal`을 재정의한다. v1 검증 완료 후 레거시 함수 삭제 여부를 별도 결정한다.

## 지도 데이터 감사 규칙

빌드 중 다음을 검사한다.

### 오류 — 빌드 중단

- `connected`가 배열이 아님
- `connected`가 존재하지 않는 맵을 참조
- `dirs`가 객체가 아님
- `dirs`가 존재하지 않는 맵을 참조

### 경고 — 빌드 계속

- `connected` 편도 연결
- `dirs` 목적지가 `connected`에 없음

편도 연결은 퀘스트/포탈 등 의도된 설계일 가능성이 있어 자동 수정하지 않는다.

## 현재 확인된 데이터 이슈

### 페이욘 → 알베르타 비대칭

`페이욘.connected`에는 `알베르타`가 있으나 `알베르타.connected`에는 `페이욘`이 없다.

현재 `알베르타`에서는 `페이욘 숲 남부`를 거쳐 페이욘으로 돌아갈 수 있으므로 게임 진행 자체는 가능하지만, 직접 연결의 의도 여부를 확인해야 한다.

판정 전까지 자동 수정하지 않는다.

## v1 검증 체크리스트

- [ ] `python build.py` 성공
- [ ] 빌드 시 지도 감사 경고 내용 확인
- [ ] `index.html`에서 전체지도 버튼 정상 동작
- [ ] 현재 위치 강조
- [ ] 주요 경로 노드 선택
- [ ] 선택 지역까지 경로 강조
- [ ] 인접 지역 `이 지역으로 이동` → 기존 `tryMove()` 정상 동작
- [ ] `던전·세부지역 표시` 토글
- [ ] PC 레이아웃
- [ ] 모바일 가로 스크롤 및 선택 패널
- [ ] 다크모드
- [ ] 기존 미니맵/구역 이동 회귀 없음

## 다음 단계

v1 검증 후 진행:

1. 발견/미발견 지도 상태
2. 미발견 지역 안개 처리
3. 카프라 이동 경로 별도 표시
4. 퀘스트/MVP/NPC 서비스 표식
5. 목적지 경로 안내 개선
6. 레거시 ASCII `showWorldMapModal()` 제거
