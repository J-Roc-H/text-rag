---
tags: [project, RPG, 룬미드가츠, dev-log]
---

** 마지막 수정 날짜 : 2026-09-13 (v9.16 — P0-02③④ / P0-03 9/14 / P1-08 / P1-07 / P4-03 / P4-01 / P4-02 종결)
** 수정 시 마다 해당 날짜 항목 업데이트하여 중간부분 스킵하고 제일 아래부분부터 읽을 것.

# 룬 미드가츠 — 주요 개발 히스토리

## 압축 안내 (2026-08-03)

2026-05-08~2026-07-10 구간(v8 이전 + 통합컨설팅 1~3차)을 세션당 1~3줄로 압축. 원본의
정확한 line 번호·정규식 패턴 등 코드 diff 수준 상세는 이 압축으로 사라짐(git 등 VCS
없음, 유일한 기록이었음 — 재발생 시 코드 재분석 필요). 다만 **재발 위험이 있는 버그
패턴**은 아래 별도 절로 보존했고, 완료된 수정 항목은 전부 바로 아래 「완료 이력 상세
표」(2026-08-13, 구 `DEVREF-E 미구현목록.md` "완료 항목" 표를 이관)에 날짜·항목코드로
남아있음(그쪽이 더 상세한 경우도 있음).
2026-07-25 이후(최근 세션, v9.04~v9.10)는 원문 그대로 유지.

## 완료 이력 상세 표 (2026-08-13, DEVREF-E에서 이관)

> DEVREF-E가 "현재 할 일" Backlog 전용 문서로 재편되며, 거기 쌓여있던 완료 항목 상세 기록을
> 전량 이곳으로 옮겼다. DEVREF-E 항목 코드(1-7, I-1, 2-5 등)는 신규 Backlog에서 P0~P5 코드로
> 바뀌었으며, 옛 코드는 신규 항목에 "기존 ID"로 보존되어 있다. 아래 표는 이관 당시 그대로이며
> 요약 없이 원문 보존(추가된 2개 행 — 2-1②·2-2(부분) — 은 이번 이관에서 분리 신설).

### 우선순위 목록에서 이동 (상세 보존, 최신순)

| #    | 항목                                                                                                                                                                                                                                                                                                 | 난이도 | 출처  | 완료일        |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- | ---------- |
| I-1  | **장비 드롭 확률 재조정** — 실측 정정: 원인은 밸런싱 아닌 데이터 결손. 몬스터 490중 179가 수기 임시엔트리(`{잡템,카드}` 2줄)라 장비를 안 떨궜던 것. 스폰 임시 96마리에 원작 드롭 주입 → 처치당 장비 기대 1.02%→3.51%, 장비0 몬스터 266→196(40.0%, Pre-Re 원작 zero-equip 38.7%와 부합 확인 — RO 원래 4할은 무장비 몹). 확률값은 원작 verbatim 유지. 명백버그 2건(포이즌스포아·베르제브브) 별도 수정 | B   | 세션  | 2026-08-12 |
| I-5  | **몬스터 이름매핑표 완성** (`데이터베이스\몬스터_이름매핑.csv`) — 확률시그니처+스탯조인 자동 → RO지식 별칭사전(`alias.py`) → 레벨·드롭 검증 리졸버. 스폰 임시몬스터 105 커버(확정75/검토21/보류9). 보류=리뉴얼/차원몹(Pre-Re CSV 부재)                                                                                                              | C   | 세션  | 2026-08-12 |
| —    | **원작 Type=Healing 소모품 11종 실제 효과 연동** — 빨간허브·옐로허브·고기·사탕·캔디스트라이퍼·꿀·마스텔라의열매·딸기·위그드라실의열매·위그드라실의씨앗·몬스터사료가 전부 `잡템`(사용 불가)으로 방치되어 있었음. rAthena 원작 스크립트 수치 그대로 `heal`/`spHeal`/`effect` 필드 주입, `type:"소모품"`으로 승격. `useItem()`에 `effect:'half'` 신규 분기 추가(위그드라실의 씨앗 전용, 원작 50% 회복 — 기존 `'full'`엔 100%만 있었음)  | B   | 세션  | 2026-08-11 |
| —    | **번역 파이프라인 중복 9건 통합** — 드롭테이블 이름과 이미 구현된 아이템 이름이 다른 표기(영문 음역 vs 순우리말, 자모 탈락)로 갈라져 각각 따로 존재하던 문제. `엘루니움`/`엘누니움`/`엘루니움 스톤`→`에르늄`/`에르늄원석`, `오리데콘`→`오리데오콘`(제련 핵심 재료 2종 모두 해당), `낡은 카드앨범`→`낡은 카드첩`, `블루 포션`→`파란포션`, `화이트/블루/그린 허브`→`하얀/파란/초록허브`, `불의 화살`→`불화살`. 스텁 정의 삭제 + 드롭테이블 키 병합(레이트 합산, 충돌 0건 확인) | B   | 세션  | 2026-08-11 |
| I-2  | **스텁 아이템 528→376종 (드롭 상위 150종 실데이터화)** — rAthena Pre-Renewal `item_db`(공식 원작 소스)를 KR↔AegisName 매핑으로 대조해 143종에 실제 `buy`/`sell`/`weight` 주입. `weightSrc` 신규값 `"official"`. 스텁이 차지하던 드롭 물량 비중 71.7%→12.8%. 상세는 개발일지 2026-08-11 v9.12 섹션 참조                                                                       | C   | 세션  | 2026-08-11 |
| 1-8  | **전승 퀘스트 (원작 3단계 체인)** — 유노 현자 메테우스 실페 1,285,000z 후원 → 이미르의 책 "몸을 맡긴다" → 발할라(DUNGEON_LOCKS 잠금) 발키리. 자격 4종(Base99·Job50·스킬포인트0·탈것해제·무게500미만·일반2차) + 미충족 시 유노로 추방. 보상 statPoints 100 + HP/SP 영구 +25%. `p.rebirth` 불리언 1회 한정                                                                          | D   | E   | 2026-08-07 |
| —    | **스탯 시스템 원작화 (1-8 선행 의존)** — 누진 비용 `floor((n-1)/10)+2`, 지급 `floor(lv/5)+3`, 초기 48/전승 100, 상한 99. 검산 `totalStatPointsAt(99,true)=1325` 원작 일치. 세이브는 전액 리셋 후 환급(`statSystemV2` 1회성)                                                                                                                   | C   | 세션  | 2026-08-07 |
| —    | **전직 트리맵** — 상단바 `🌳 전직트리`. 40직업 전체를 일반/전승 2섹션 + 계열 탭, SVG 연결선, 노드 6상태, 상세 패널(조건·장소·NPC·시험·보상·택일). 시험 요약은 `q.steps` 자동 생성                                                                                                                                                                          | C   | 기획  | 2026-08-07 |
| —    | **드롭·무게 데이터 복구** — 드롭표와 아이템DB의 번역 파이프라인 불일치(`면셔츠[1]` vs `코튼셔츠`)로 드롭 엔트리 65%가 유령이던 문제. 별칭 247 + 신규장비 101종 + 스텁 528종. **드롭 장비 57→286종**, 유령 1,748→0. 무게 2,745종 주입                                                                                                                                    | D   | 세션  | 2026-08-07 |
| A~L  | **전직·전승 엔진 결함 12종** — `advanceJobQuestStep` 미정의(전직퀘 전반 파손) · job3 13종 `reqJob` 이 비전승 2차라 도달 불가 · `prevJobCode` 키 불일치 · 노비스 하이가 일반 1차로 이탈 · job바 cap 오판 · 전승 Job Lv 미검사 · `dropSlots` 데드코드 · `parseItem` 정규식 · `p.settings` 소실 · 상자명 불일치                                                            | C   | 세션  | 2026-08-07 |
| 1-7  | **카드 효과 구현 76개** — `desc:"(미구현)"` 카드 절반 가까이 장착해도 효과 없던 문제. 핵심 컨텐츠. Phase 3 전수 구현 완료, 미구현 0개                                                                                                                                                                                                     | C   | E   | 2026-07-25 |
| 2-5  | **G-3 제조 스킬 재료/성공률 시스템** — 파머시·단검~창 제작 7종·철/강철/속성석 제조·화살 제조 전부 재료 미소모·성공률 없던 문제. `mats:{}` 필드 + `consumeMats()` 헬퍼로 완성                                                                                                                                                                              | C   | 컨   | 2026-07-25 |
| 3-13 | **월드맵 동서남북 이동 구조 리팩토링** — 허브 필드를 방향별 분할(프론테라 평원 4분할, 소그라트 사막 2분할, 페이욘 숲 2분할) + 전 맵 `dirs` 부여 + 방향키/나침반 이동 + 레거시 맵명 마이그레이션 (설계안 1안 전체 적용, `connected` 하이브리드 유지). 던전 층간 이동은 2차 제외                                                                                                                        | D   | 기획  | 2026-07-25 |
| 2-1②  | **스틸 코인 인플레이션 억제** — Lv10 스틸 코인이 Lv50 몹에서 2,500~7,500z/회(쿨4초)로 수익 과다 → 수익식 `t.lv×slv×2` 변경 + 1몹 1회 제한 적용. (부모 항목 2-1의 ①노점비율 ③매머나이트는 미완료 — DEVREF-E P1-05/P1-06으로 계속) | B | 컨 | 2026-06-27 |
| 2-2(부분) | **ASPD 만능화 — 자연회복 실시간 분리** — 자연회복(HP/SP)만 1000ms 실시간 기준으로 분리 완료(상세: 위 2026-07-25 v9.06 섹션). 스킬쿨·화살SE는 여전히 ASPD 비례 가속 — 잔여는 DEVREF-E P1-01로 계속 | C | 컨 | 2026-07-25 |
| 1-3  | **B-8 명중 공식 flee/hit 무시** — 플레이어→몬스터 명중식이 `80+(hit-t.lv-t.lv×0.3)`로 몬스터 `flee` 필드를 무시하고 레벨 근사치를 사용 중이었음. `80+(hit-flee)` 원작 공식으로 수정(실전투 + 전투 시뮬레이터 3곳). 부가로 반대 방향(몬스터→플레이어) 명중식도 몬스터 `hit` 필드 대신 레벨을 쓰던 동일 패턴 버그 발견, 같이 수정(실전투+시뮬레이터+오프라인 틱 3곳). 총 6곳                                              | C   | 컨   | 2026-07-10 |
| 2-8  | **전투 스킬 로테이션 핫바 연동** — `processTurn` 공격/힐/버프/fallback 풀 전부 핫바 등록 스킬로 제한 (`hotbarSkillSet`). 핫바 스킬 0개면 평타만. 티어 큐 로직은 유지                                                                                                                                                                             | C   | 기획  | 2026-07-02 |
| 2-9  | **미니필드 세분화 + 리젠 로직 개편** — 범용 자동 생성 방식으로 구현: `getZones(map)`이 `md.monsters` 레벨순 정렬→균등 분할(A~D). 맵 명시 `zones` 필드 우선. 50킬 자동 전진(B안), 리젠 10~20초 무작위, 핫바 '구역 전진/후퇴' + 지도 모달 구역 카드 클릭 이동(`setZone`). 상세는 DEVREF-G                                                                                         | E   | 기획  | 2026-07-02 |
| 3-10 | **몰래하기 모드 완성** — 엑셀 위장 오버레이로 구현. 백틱(`) 보스키, 상태표시줄 로그 한 줄, HP/SP/제니/EXP 셀 매핑. 구형 흑백(work-mode) CSS는 데드코드로 잔존                                                                                                                                                                                        | B   | E   | 2026-07-02 |
| 1-1  | **완전회피(perfectFlee) 미반영** — 루나틱·포링·요요·초코·토드 카드 등 10여 종에 수치 기재됐으나 `calcStats`·`processTurn` 미연동. 장착해도 효과 없음                                                                                                                                                                                         | B   | E   | 2026-06-27 |
| 1-2  | **B-6 탈것(mount) 공격력 0** — `bonus.atk`에 쌓이지만 `totalAtk` 계산에 미합산 → 페코페코/마도기어 장착 의미 없음                                                                                                                                                                                                                | B   | 컨   | 2026-06-27 |
| 1-4  | **B-11 exp 테이블 꼬리 이상** — `[97]=211,651,644 → [98]=9,999,999` 급락, `[99]=0` 뒤 미사용 5항목(6.5M/8M/12M/34M/0) 잔존. Lv97→Lv98 전이 이상                                                                                                                                                                         | B   | 컨   | 2026-06-27 |
| 1-5  | **B-13 퀘스트 보상 jexp 잡레벨업 미실행** — `giveQuestReward`의 jexp는 p.jobExp에만 더하고 잡레벨업 루프 미실행. 다음 처치 때까지 레벨업 지연                                                                                                                                                                                              | B   | 컨   | 2026-06-27 |
| 1-6  | **B-14 고목나무 가지 MVP isMvp=false** — slaves 없는 MVP(도플갱어 등)가 isMvp 플래그 없이 소환됨 → 즉사 면역 미적용, 보스 판정 오류                                                                                                                                                                                                   | B   | 컨   | 2026-06-27 |
| 2-4  | **C-7 영구 디버프 문제** ① 스톤 커스: `t.def=0` 턴 제한 없이 영구 적용. ② 콰그마이어: `m.lv` 영구 감소 → 스틸코인 수익·명중에 부수효과. ③ 디스펠: 몬스터 전체 SE 제거 → 자기가 건 독/스턴도 지워지는 자해                                                                                                                                                            | B   | 컨   | 2026-06-27 |
| 3-1  | **G-2 디스카운트/오버챠지 비가시** — 적용은 정상이나 상점 UI에 표기 없어 플레이어 인지 불가. 원가 취소선 + 할인율 뱃지 표시 권고                                                                                                                                                                                                                   | B   | 컨   | 2026-06-27 |
| 3-2  | **C-6 힐 healPick 회복량 추정 오류** — 응급치료·리저렉션에도 힐 공식 적용 → 응급치료 과소평가(선택 안 됨), 리저렉션 오인 선택. 자동사냥 AI 품질 저하                                                                                                                                                                                                  | B   | 컨   | 2026-06-27 |
| D    | **오프라인 보상 문구 괴리** — "(최대 5분치 적용)" vs 실제 캡 30,000틱(100분). 위치 ~9891/9931 (v8 기준)                                                                                                                                                                                                                     | —   | 컨D  | 2026-06-27 |

### 기존 완료 이력

| 항목 | 출처 | 완료일 |
|---|---|---|
| 사망 시 경험치 손실 (Pre-Re: 현재레벨 EXP 1%) | E | 2026-06-21 |
| 제련 시스템 보완 (등급별 재료·단계별 성공률·+10 캡) | E+컨A-3 | 2026-06-21 |
| 즉사 스킬 MVP 보스 면역 (타로카드·아브라카다브라) | 컨C-1 | 2026-06-21 |
| 몽크 흡기 SP 수급 과다 수정 (orbs×8+slv×4) | 컨C-2 | 2026-06-21 |
| 아이템 타입 문자열 통일 ('소비'→'소모품' 7곳) | 컨A-1 | 2026-06-20 |
| G.cooldowns 단위 분리 (아이템=타임스탬프, 스킬=틱) | 컨A-2 | 2026-06-21 |
| db-cjb 키 통일 (한글 7종→JOB 코드) + HP/SP 계수 전승 2차 추가 | 컨A-4+B-1 | 2026-06-20 |
| 잡레벨 상한 공용 함수 getJobLvCap() + 몬스터 사망 처리 공용화 | 컨A-5+B-3+B-4 | 2026-06-21 |
| 노비스 기본 스킬 키 통일 ("기본 스킬") | 컨A-6 | 2026-06-20 |
| 세이프티 월 SE 수정 (safetyWall={count,turns}) | 컨A-7 | 2026-06-20 |
| 오토 스펠 발동 풀 명칭 수정 (라이트닝 볼트·화이어 볼) | 컨B-2 | 2026-06-20 |
| SE 덮어쓰기 패턴 수정 (프로스트 다이버·홀리 크로스·타로카드) | 컨B-5 | 2026-06-20 |
| 카프라 창고 safeK 이스케이프 적용 | 컨B-7 | 2026-06-21 |
| equipItem 수량 가드 + 양손 무기 방패 자동 해제 | 컨B-9 | 2026-06-21 |
| 보스 스킬 분기 race 필드 수정 (element→race) | 컨B-12 | 2026-06-20 |
| 노점 개설 2중 버그 (opensModal + setInterval 이전) | 컨G-1 | 2026-06-21 |
| no-op/제조 스킬 자동 로테이션 제외 (_NC Set) | 컨G-4 | 2026-06-21 |
| 다크 모드 구현 (CSS 변수 오버라이드 + 모달 내 인라인 스타일) | — | 2026-06-20~21 |
| 맵별 출현 몬스터 정비 | E | 2026-06-10 |
| 카프라 워프 서비스 | E | 2026-06-10 |
| 자동사냥 버튼 상태 동기화 버그 수정 | — | 2026-06-10 |
| 사냥 방식 3종 (소극적/공격적/몰이) | E | 2026-06-10 |
| 보스 슬레이브 스폰 시스템 | E | 2026-06-10 |
| 선물상자·오래된상자·보물상자 랜덤 드랍 | E | 2026-06-10 |
| 낡은 카드첩·고목나무 가지 효과 | E | 2026-06-10 |
| 일 대 다수 전투 체계 (AOE, HP최저 우선 타겟) | E | 2026-06-10 |
| 지나가던 유저 시스템 | E | 2026-06-10 |

### 스킬 DB 현황 관련 참고

전 직업 스킬 구현 현황·effect 미구현 분류는 `DEVREF-D 퀘스트 스킬 전투.md`「스킬 시스템 현황」으로 이관됨(2026-08-13). 스킬 퀘스트 10개·체인 퀘스트 14개 상세 표도 동 문서로 이관.

## 자주 나온 버그 패턴 (압축 중 발견, 재발 방지용)

- **`window.$()`는 요소 없으면 null이 아니라 truthy 더미 Proxy 반환** → 존재 체크에
  쓰면 항상 참 → `document.getElementById()`로 체크할 것 (2026-07-03, 몰래하기모드 버튼 무반응 원인)
- **아이템 키에 `<>` 포함**(카드 합성명 `"장비 [1] <카드>"` 등)이 `onclick` 속성이나
  `innerHTML`에 그대로 삽입되면 HTML 파서가 태그로 오인해 DOM 파괴 → 항상
  `.replace(/</g,'&lt;').replace(/>/g,'&gt;')` escape 후 삽입 (2026-06-08~09, 5개 함수에서 반복 발견)
- **DB에 없는 compound 키는 `DB.items[k]` 직접 참조하면 실패** → 항상 `parseItem(k)`
  경유해서 `.base` 사용할 것 (2026-06-08~09, showItemDetail/showInvModal/openEquipModal 등 5곳 반복)
- **정규식 greedy 그룹이 뒤 optional 그룹을 못 먹게 막을 수 있음** → lazy(`+?`) +
  `$` 앵커로 전체 문자열 끝까지 강제 확장 (parseItem 카드 파싱 버그, 2026-06-09)

## 2026-05 ~ 2026-06-11 — 초기 구현 (압축)

- 05-08 기획 시작 — 직장인용 방치형 텍스트 RPG, 라그나로크 온라인 스타일 채택
- 05-10~12 초기 구현 — HTML 기본구조, 노비스/마법사 스킬 DB
- 05-13 마법사 빌드 — 스텟 배분 로직, 시스템 대폭 업데이트
- 05-20 모듈화 — 단일 HTML → CSV/JS 분리(토큰 효율화 목적, 이후 다시 단일 HTML로 회귀)
- 05-27~28 NPC 시스템 — 상점/카프라, 모달 버그 수정
- 05-29 버그 집중 수정 — 전승전직/상점표시/인벤분류/최소화중 사냥중단
- 05-30 신규 시스템 — 액티브 스킬 자동사용 토글, 카드 시스템 설계 착수
- 05-31 튜토리얼 5단계 + 에디터 전직 기능
- 06-01~02 데이터 보강 — 몬스터DB 누락 보충, 카드드랍 데이터, 원거리 공격 구현
- 06-03 우선순위 정리 + 에디터 조건부 해금 시스템
- 06-05 카드시스템 완성(175개+미구현246개) · 퀘스트 대확장(A등급10개+B등급16개, 신규NPC36명) ·
  던전잠금 7종 · 우탄족 언어장벽 · 자동전투중 대화차단
- 06-08 (대규모, ~15개 항목) 스킬DB 전면개정(레퍼런스v13, 미구현스킬 전직업 ~150종 추가+선행조건
  전수점검) · DB뷰어(5탭) · 직업별무기제한+신규무기25종 · 노점시스템(NPC시뮬 방식) ·
  패시브A~E 전면구현(Edit1~12) · 액티브스킬 전면구현(SE 17종+) · 카드소켓 DB기반 재설계 ·
  카드합성 관련 HTML인젝션 버그 다수(위 버그패턴 참조) · 에디터 장비/아이템/카드 지급 기능 ·
  옵션 시스템 확장 4종 + 에디터 스킬MAX
- 06-09 카드DB 전수검증 + 누락몬스터 75종 추가 · compound 아이템 표시/장착 버그 5연속
  수정(위 버그패턴 참조, showItemDetail/showInvModal/updateUI/openEquipModal 등)
- 06-10 지나가던 유저 시스템(MMORPG 연출) · 자동사냥 버튼 동기화 버그 · 맵별 출현몬스터
  전면정비(20맵 hasMvp + 28맵 구성, 원작 기준) · 사냥방식 3종(huntStyle) · 카프라워프 서비스 ·
  보스 슬레이브 스폰 · 보물상자/카드첩/고목나무가지 효과 · 다수전투 타겟팅+AOE킬 처리 완성
- 06-11 지나가던유저 재등장 버그(전투중 재시도 무한차단) · 노비스전직 스킬포인트 체크 누락 ·
  스킬사용 SP소모 로그 · 마니피캇 자연회복 로그 · 웨폰퍼펙션 실제 적용 · 화살 시스템 구현(궁수
  DEX데미지 미반영 버그 동시 발견/수정, "활" 아이템 DB 자체가 없었음) · 화살 34종 전면 확장 ·
  전직퀘스트 모달 미표시 · 2차전직 상호배타 시스템 · 오프라인보상 후 사냥맵 복귀 버그 ·
  버프만료 알림 개선 · 힐 SP낭비 방지(임계값 옵션화) · 몬스터DB 외부데이터 변환(490종, 게임
  본체 미반영 별도 산출물)

## 2026-06-12 ~ 2026-06-21 — v8 통합 컨설팅 + 버그패치 1~2차 (압축)

**06-12**: v8 13,311줄 전수 정독 + 핵심 수식 수동 검증(코드 무수정). 치명버그 7건·중요버그
14건·밸런스 7건 발견 → `룬미드가츠_v8_통합컨설팅_2026-06-12.md`. 아이템타입 문자열 불일치,
cooldowns 단위혼용, refineLevel 키불일치, db-cjb 키혼용, 전승직 잡렙상한 리스트 4곳 제각각,
"기본 스킬" 키불일치, 세이프티월 유령SE 등 핵심 발견. 민스트럴→클로운 명칭 통일(코드명 유지).

**06-20~21**: v9.01 패치 — 위 발견사항 전부 수정(A-1~A-7, B-1~B-12, G-1~G-4, C-1~C-2) +
다크모드 구현 + 사망시 EXP손실(Pre-RE) + 제련시스템 보완(등급별재료·단계별성공률·+10캡).
**전부 위 「완료 이력 상세 표」〉「기존 완료 이력」(구 `DEVREF-E 미구현목록.md`에서 2026-08-13
이관)에 항목별 날짜로 기록됨 — 코드 diff 필요시 그 표의 항목명으로 재작업할 것(예: db-cjb 키
통일, G.cooldowns 단위분리 등).**

## 2026-06-27 ~ 2026-07-10 — v9.02~v9.03 버그패치 3차 + 기능 확장 (압축)

**06-27 v9.02 (3차 통합패치)**: 완전회피 카드보너스 미반영, 탈것ATK 미합산, EXP테이블
Lv98 오류, 퀘스트jexp 잡레벨업 누락, 고목나무가지 MVP판정 누락, 스틸코인 인플레이션,
영구디버프 3종(스톤커스/콰그마이어/디스펠), 상점 할인율 UI미표시, healPick 로직 오류,
오프라인 메시지 오류 — **전부 위 「완료 이력 상세 표」(구 DEVREF-E 항목코드 1-1,1-2,1-4~1-6,
2-1②,2-4,3-1,3-2,D)와 1:1 대응.**

**06-27 파리/나비의 날개 버그**: 마을에서 파리날개 써도 소모됨, savedMap이 필드일 때
나비날개가 필드로 이동, saveGame이 필드저장시 savedMap 무조건 덮어씀 — 3건 모두 safe맵
체크 추가로 수정.

**06-28 핫바 시스템**: 12슬롯(QWERASDFZXCV) 신규 구현, 스킬/아이템/도망 등록·실행. 상세: DEVREF-F.

**06-29~06-30 몬스터 DB 인게임 편집기**: 490마리 검색+개별편집(기본/방어전투/어빌리티/
속성종족크기/경험치제니/드롭/출몰지역) + HTML 파일로 내보내기. 06-30에 UI 2컬럼화+진입점
이동. 상세: DEVREF-F.

**07-02 (다수 세션)**: 전투스킬 핫바연동(2-8, 핫바 미등록시 자동전투 평타만) · 구역세분화+
리젠개편(2-9, `getZones()` 자동생성 방식) · UI개선 6종(로그필터/구역HUD/핫바상태/장비비교/
모바일레이아웃/몬스터패널) · 몰래하기모드 완성(3-10, 엑셀위장 오버레이) · 화면스타일 저자극화
4종 · 구역시스템 피드백반영(리젠 30초→10~20초 무작위, 지도에 구역카드) · **3-13 월드맵
동서남북 설계문서 작성**(`설계_월드맵_동서남북_3-13.md`, 구현은 07-25). 부수발견: 알베르타↔
페이욘숲 편도연결 버그(핫픽스 후보로 기록, 07-25 리팩토링 때 해소 확인됨).

**07-03**: DEVREF-E 문서정리(완료항목 이관) · 몰래하기모드 버튼무반응 버그(`$()` truthy
함정, 위 버그패턴 참조) + 복귀UX 개선 · 옵션 환경/전투 설정 분화 + 리젠·구역전환 플레이어
설정화 · 업무모드 내 저장·종료 기능(3-10 확장, Ctrl+S 가로채기).

**07-06**: 방어구·악세서리 DB 전면교체(원작 데이터 1,310행 → 약 1,040항목 등록, reqLv
필드 추가했으나 엔진 미사용) · 스테이터스창 장비기여분 표시 확장(HIT/FLEE/CRIT 장비몫 분리,
음수보너스 빨강표시).

**07-10 명중공식 flee/hit 미반영 수정 (DEVREF-E 1-3)**: 플레이어↔몬스터 양방향 명중식이
`flee`/`hit` DB 필드를 안 읽고 레벨 근사치를 쓰던 버그. `80+(HIT-FLEE)` 원작 공식으로
6곳(실전투 2+시뮬레이터 3+오프라인틱 1) 통일 수정.

---

## 2026-07-25 — v9.04 카드 효과 시스템 구현 (1-7, 샘플 단계)

파일: `룬미드가츠_v9.04.html`

### 배경
DEVREF-E 1순위 "**1-7 카드 효과 구현 76개**" — 약 175개 카드 중 절반이 `desc:"(미구현)"`으로 마킹되어 장착해도 게임상 효과가 없음. 핵심 컨텐츠.

### Phase 1: 샘플 카드 시스템 구현 (완료)

#### ① db-items 수정: 샘플 9개 카드에 effect 필드 추가
- 루나틱 카드: `{"effect":{"type":"stat","stats":{"luk":1},"bonus":{"crit":1,"pd":1}}}`
- 드롭스 카드: `{"effect":{"type":"stat","stats":{"dex":1},"bonus":{"hit":3}}}`
- 파브르 카드: `{"effect":{"type":"stat","stats":{"vit":1},"bonus":{"maxHp":100}}}`
- 호넷 카드: `{"effect":{"type":"stat","stats":{"str":1},"bonus":{"atk":3}}}`
- 고렘 카드: `{"effect":{"type":"special","weaponUnbreakable":true,"bonus":{"atk":5}}}`
- 스켈레톤 카드: `{"effect":{"type":"mixed","bonus":{"atk":10},"seProc":{"se":"stun","chance":0.02,"turns":3}}}`
- 세비지 베베 카드 (미구현): `{"effect":{"type":"seProc","se":"stun","chance":0.05,"turns":3}}`
- 드라큘라 카드 (미구현): `{"effect":{"type":"lifesteal","spRate":0.05,"chance":0.10}}`
- 프리오니 카드: `{"effect":{"type":"stat","bonus":{"hit":100}}}`

#### ② calcStats() 수정 (6453줄 이후): 카드 effect 처리 로직
- **stat type**: 기본 스탯 (STR/AGI/VIT/INT/DEX/LUK) 추가
- **special type**: 무기 손상 무시 등 특수 효과
- **raceBonus type**: 종족별 추가 데미지 (processTurn 예약)
- **seProc type**: 상태효과 확률 (processTurn 예약)
- **lifesteal type**: SP 흡수 (processTurn 예약)
- **bonus 필드**: 파생 스탯 (ATK, HIT, CRIT, 완전회피, MaxHP, MaxSP) 직접 추가

### Phase 2: processTurn() 카드 effect 처리 (완료)

**구현 위치**: 8019줄~8053줄 (평타 명중 → 더블어택 → [카드 effect] → 로그)

**구현 내용:**
- **raceBonus type**: 특정 종족에게 추가 데미지 (e.g. 포링 카드 동물형 +20%)
  - 데미지 재계산 후 몬스터 HP 차감
  - 로그: "🃏 [카드명] [동물형] 추가 데미지 +NNN"
  
- **seProc type**: 상태효과 확률 발동 (스턴, 포이즌 등)
  - 확률 체크 후 t.statusEffects에 추가
  - 로그: "🃏 [카드명] 💫 스턴 부여! (3턴)"
  
- **lifesteal type**: SP 흡수 (드라큘라 카드 등)
  - 확률 체크 후 p.sp 회복
  - 로그: "🃏 [카드명] SP 흡수 +NNN"

**테스트 대상:**
1. 세비지 베베 카드 (seProc): 5% 확률로 스턴 발동
2. 드라큘라 카드 (lifesteal): 10% 확률로 SP 흡수
3. 포링 카드 (raceBonus): 동물형에게 +20% 데미지

---

## 2026-07-25 — v9.06 ASPD 만능화 문제 해결 (2-2)

파일: `룬미드가츠_v9.06.html`

### 배경
DEVREF-E 2순위 "**C-4 ASPD 만능화**" — 자연회복(턴당)·스킬쿨(틱)·화살SE proc가 ASPD에 의해 모두 가속됨. 고AGI 캐릭터 피격률 11% → 사실상 무적 상태. 원작 Pre-RE는 자연회복이 턴 독립적이어야 함.

### 문제 분석

**원인:** processTurn()의 자연회복이 turnCount 기반

```javascript
// v9.05 (잘못된 로직)
let turnHpRec = s.hpRegen;      // VIT 기반 회복량
let turnSpRec = s.spRegen;      // INT 기반 회복량
// ↓ 매 턴마다 회복 (ASPD가 빠르면 턴도 빨리 진행)
if(p.hp < p.maxHp) { p.hp += turnHpRec; }
if(p.sp < p.maxSp) { p.sp += turnSpRec; }
```

**영향:**
- ASPD 높음 = processTurn() 호출 빈도 증가 = 자연회복도 비례적으로 증가
- 고AGI 캐릭터는 일정 시간에 훨씬 많은 회복
- 결과: 고AGI 캐릭터가 거의 불사 상태 (체력 계속 채워짐)

### 해결책: 실시간 초 기반 자연회복 분리

```javascript
// v9.06 (수정된 로직, 라인 25731~25760)
// [2-2 FIX] 자연회복: 실시간 초 기반 (ASPD 무관)
let now = Date.now();
if (!p.lastHpRegenTime) p.lastHpRegenTime = now;
if (!p.lastSpRegenTime) p.lastSpRegenTime = now;

// HP 회복: 1000ms마다 (ASPD와 무관)
if (now - p.lastHpRegenTime >= 1000) {
  if(p.hp < p.maxHp) {
    let recVal = s.hpRegen;
    actHp = Math.min(recVal, p.maxHp - p.hp);
    p.hp += actHp;
    p.lastHpRegenTime = now;  // 타이머 리셋
  }
}

// SP 회복: 1000ms마다 (ASPD와 무관)
if (now - p.lastSpRegenTime >= 1000) {
  if(p.sp < p.maxSp) {
    let recVal = s.spRegen;
    actSp = Math.min(recVal, p.maxSp - p.sp);
    p.sp += actSp;
    p.lastSpRegenTime = now;  // 타이머 리셋
  }
}
```

### 영향
- **ASPD가 높아도 자연회복은 정확히 1초마다 1회**
- 고AGI 캐릭터가 더 이상 자동으로 불사가 되지 않음
- 밸런스 개선: 전투 시간이 길어질 수 있으나 의도적

### 향후 개선 사항
- 상태효과 지속시간: 턴 기반 유지 (향후 초 기반으로 리팩토링 가능)
- 스킬쿨: 틱 기반 유지 (현재 시스템 유지)
- 2-3 (전투 중 자연회복 정지): 별도 과제로 미연기

---

## 2026-07-25 — 3-13 월드맵 구조 대대적 리팩토링

### 프론테라 지역 분할 (3-13 설계안 적용)

#### 1. 프론테라 마을 분할 (사용자 추가 요구)
- **프론테라** (중심): 기본 마을, dirs={north/south/east/west}로 4방향 평원 연결
- **프론테라 왕궁** (신규 마을, 서쪽): 기사단 본부 (기사단장 허먼 배치)
  - connected: ["프론테라"], dirs: {"east": "프론테라"}
- **프론테라 성당** (신규 마을, 동쪽): 신앙 중심지
  - connected: ["프론테라"], dirs: {"west": "프론테라"}
  - NPC 이전: 성직자, 대주교 이오렌, 수련 대사, 대성직자 레이웬 → 성당 배치

#### 2. 필드 분할 (설계안 1안 전체 적용)

**프론테라 평원 → 4분할 (필수)**
| 신 맵 | Lv | 몬스터 | dirs |
|---|---|---|---|
| 프론테라 남쪽 평원 | 1~5 | 1,2,3,4,5,6 | N:프론, S:이즈, W:사막동 |
| 프론테라 서쪽 평원 | 4~12 | 10,12,14 | E:프론, W:게펜필 |
| 프론테라 동쪽 평원 | 9~18 | 14,15,13 | W:프론, S:숲북 |
| 프론테라 북쪽 평원 | 15~25 | 13,15,46 | S:프론, N:묘르닐 |
- 미궁 숲 입구 위치 이전: 평원→북쪽 평원

**모로크 사막 → 2분할 (필수)**
- **소그라트 사막 동부** (Lv 6~27): 9,21,69,123,125 / dirs={E:프론남, W:사막서}
- **소그라트 사막 서부** (Lv 30~48): 60,61,68,124,125 / dirs={E:동부, W:모로크, S:코모도} / MVP 198
- 기존 "모로크 사막" 맵명 완전 제거 & 대체

**페이욘 숲 → 2분할 (권장)**
- **페이욘 숲 북부** (Lv 16~25): 13,34,36 / dirs={N:프론동, S:숲남}
- **페이욘 숲 남부** (Lv 25~32): 35,39,40 / dirs={N:북부, S:페이욘, E:알베} / MVP 196

#### 3. 전체 맵 dirs 필드 추가 (설계안 §2 준수)

**원칙:**
- 상호성 필수: A→B면 B→A 필수
- dirs⊆connected (dirs는 connected의 부분집합)
- 던전은 dirs 미적용 (connected만 유지)

**중앙권 (프론테라~게펜)**
- 게펜: dirs={E:게펜필, S:묘르닐}
- 묘르닐 산맥: dirs={S:프론북, W:게펜, N:알데}
- 게펜 필드: dirs={E:프론서, W:게펜, S:오크}
- 오크 마을: dirs={N:게펜필}

**동부·남부 (이즈루드~알베르타)**
- 이즈루드: dirs={N:프론남}
- 페이욘: dirs={N:숲남, E:알베}
- 알베르타: dirs={W:숲남}

**남부 사막·정글권 (소그라트~코모도)**
- 소그라트 사막 동부: dirs={E:프론남, W:사막서}
- 소그라트 사막 서부: dirs={E:동부, W:모로크, S:코모도}
- 코모도 필드: dirs={N:사막서, S:움발라필}
- 코모도: dirs={N:코모도필}
- 움발라 필드: dirs={N:코모도필, S:움발라}
- 움발라: dirs={N:움발라필}

**북부·동부 (알데바란~휘겔)**
- 알데바란: dirs={S:묘르닐, E:슈발, N:루티에}
- 루티에: dirs={S:알데}
- 슈발츠발드 국경: dirs={W:알데, E:유노필}
- 유노 필드: dirs={W:슈발, S:유노}
- 유노: dirs={N:유노필, E:아인, W:휘겔}
- 휘겔 필드: dirs={E:유노}
- 휘겔: dirs={W:휘겔필}
- 아인브로크 필드: dirs={W:유노, S:리히필}
- 아인브로크: dirs={N:아인필}
- 리히타르젠 필드: dirs={N:아인필, S:라헬필}
- 리히타르젠: dirs={S:리히필}
- 라헬 필드: dirs={N:리히필, S:베인필}
- 라헬: dirs={N:라헬필}
- 베인스 필드: dirs={N:라헬필, S:베인}
- 베인스: dirs={N:베인필}

**이세계권 (차원 균열 경유)**
- 이세계 캠프(아쉬바쿰): dirs={S:스플렌, W:마누크}
- 스플렌디드 필드: dirs={N:아쉬바}
- 마누크 필드: dirs={E:아쉬바}

#### 4. 레거시 마이그레이션 로직 (loadCharacter 함수)

```javascript
const MAP_RENAME = {
  "프론테라 평원": "프론테라 남쪽 평원",
  "모로크 사막": "소그라트 사막 동부",
  "페이욘 숲": "페이욘 숲 북부"
};
// p.savedMap, p.unlockedMaps 키 자동 변환
```

**영향**: v9.06 저장본 로드 시 자동 변환. 기존 플레이 데이터 무결성 보장.

#### 5. 하드코딩 스트링 전수 수정

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| title-map 초기값 | 프론테라 평원 | 프론테라 남쪽 평원 |
| G.currentMap 기본값 | 프론테라 평원 | 프론테라 남쪽 평원 |
| 캐릭터 생성 기본값 (마을) | 프론테라 | 프론테라 (유지, 마을) |
| 지도 텍스트아트 (×2) | 프론테라 평원 | 프론테라 남쪽 평원 |
| 튜토리얼 퀘스트 텍스트 | 프론테라 평원의 포링 | 프론테라 남쪽 평원의 포링 |

#### 6. 데이터 정합성 검증 완료

✓ 모든 connected 배열 유효성 검사 (맵명 오탈자 없음)
✓ dirs 상호성 검증 (모든 연결이 양방향)
✓ dirs⊆connected 검증 (모든 dirs가 connected에 포함)
✓ 기존 던전 입구 연결 유지
✓ 오류 발견 및 핫픽스: 알베르타↔페이욘 숲 남부 상호 연결 확인

#### 7. NPC 재배치 (db-npcs)

| NPC | 이전 위치 | 신규 위치 | 역할 |
|---|---|---|---|
| 기사단장 허먼 | 프론테라 | 프론테라 왕궁 | 소드맨 전직 |
| 성직자 | 프론테라 | 프론테라 성당 | 복사 전직 |
| 대주교 이오렌 | 프론테라 | 프론테라 성당 | 크루세이더 전직 |
| 수련 대사 | 프론테라 | 프론테라 성당 | 몽크 전직 |
| 대성직자 레이웬 | 프론테라 | 프론테라 성당 | 프리스트 전직 |

### 산출물
- ✓ v9.07.html 생성 (db-maps/db-npcs 완전 개편)
- ✓ 마이그레이션 로직 구현 (loadCharacter)
- ✓ 레거시 데이터 자동 변환 (savedMap/unlockedMaps)
- ✓ 개발일지 업데이트
- ⏳ DEVREF-E 3-13 상태 변경 (별도 수행)

---

## 2026-07-25 — v9.08 제조 스킬 재료·성공률 시스템 구현 (2-5)

### 개요
12개 제조 스킬(파머시·단검~창 제작·철/강철/속성석 제조·화살 제조)에 **재료 소비 및 성공률 시스템** 추가

### 구현 내용

#### 1. consumeMats() 헬퍼 함수 추가
```javascript
function consumeMats(p, mats) {
  // p.inventory에서 mats 객체의 재료들을 소비
  // 재료 부족 시 false 반환, 로그: "❌ 재료 부족: 철 5개 필요 (현재 3개)"
  // 성공 시 true 반환 후 인벤토리에서 제거
}
```

#### 2. 제조 스킬별 재료 정의 (mats 필드)

**블랙스미스(화이트스미스) 무기 제작 스킬 (7개)**

| 스킬 | Lv1 | Lv2 | Lv3 | 성공률 기본값 |
|---|---|---|---|---|
| 단검 제작 | 철×2 석탄×1 | 철×3 석탄×1 엘누니움×1 | 철×4 석탄×2 엘누니움×2 | 70% |
| 검 제작 | 철×5 엘누니움×2 | 철×6 엘누니움×3 오리데오콘×1 | 철×7 엘누니움×4 오리데오콘×2 | 65% |
| 양손검 제작 | 철×8 엘누니움×3 | 철×10 엘누니움×5 오리데오콘×1 | 철×12 엘누니움×7 오리데오콘×3 | 60% |
| 도끼 제작 | 철×4 석탄×2 | 철×5 석탄×3 엘누니움×1 | 철×6 석탄×4 엘누니움×2 | 70% |
| 너클 제작 | 철×2 엘누니움×1 | 철×3 엘누니움×2 오리데오콘×1 | 철×4 엘누니움×3 오리데오콘×2 | 75% |
| 메이스 제작 | 철×3 석탄×2 | 철×4 석탄×3 엘누니움×1 | 철×5 석탄×4 엘누니움×2 | 70% |
| 창 제작 | 철×6 엘누니움×2 | 철×8 엘누니움×3 오리데오콘×1 | 철×10 엘누니움×4 오리데오콘×2 | 65% |

**블랙스미스 재료 제조 스킬 (3개)**

| 스킬 | 입력 | 출력 | 성공률 기본값 |
|---|---|---|---|
| 철 제조 (Lv1~5) | 철광석×(1+Lv) | 철×Lv | 85% |
| 속성석 제조 (Lv1~5) | 속성석 원석×(2+Lv) | 속성석×Lv (무작위) | 80% |
| 강철 제조 (Lv1~5) | 철×(1+Lv) + 석탄×1 | 강철×Lv | 75% |

**알케미스트 스킬 (1개)**

| 스킬 | 입력 | 출력 | 성공률 기본값 |
|---|---|---|---|
| 파머시 (Lv1~10) | 빈 병×3 + 약초×1 | 포션×(1+⌊Lv/3⌋) | 60% → 95% (+3% per Lv) |

**헌터 스킬 (1개)**

| 스킬 | 입력 | 출력 | 성공률 기본값 |
|---|---|---|---|
| 화살 제조 (Lv1) | 목재×5 + 깃털×1 | 화살×100 | 80% |

#### 3. 성공률 로직

**공식:**
```
successRate = baseRate + (slv - 1) × perLv + (DEX / 50) × 5 + (LUK / 50) × 3
successRate = Math.max(10, Math.min(100, successRate))  // 10~100% 클램프
```

- **기본 성공률**: 스킬별 상이 (60~85%)
- **스킬 레벨 보정**: 레벨당 +2~3%
- **DEX 보정**: (DEX/50) × 5% (최대 +5%)
- **LUK 보정**: (LUK/50) × 3% (최대 +3%)
- **최소/최대**: 10% ~ 100%

**예시:**
- 단검 제작 Lv2 (기본 70% + 2%)
  - DEX 50: 72% + 5% = 77%
  - LUK 50: 77% + 3% = 80%
  - 최종: 80%

#### 4. 각 스킬 effect 함수 수정

**구조 (예: 단검 제작)**
1. mats 필드로부터 재료 요구량 계산
2. consumeMats(p, mats) 호출 → 실패 시 에러 로그 반환
3. 성공률 계산 (위 공식)
4. Math.random() 검사 → 실패 시 실패 로그 반환
5. 아이템 추가 후 성공 로그 반환

**로그 형식**
- 성공: `🗡️ [단검 제작 Lv.X] [나이프] 제작 성공! (성공률 80%)`
- 실패: `❌ [단검 제작 Lv.X] 제작 실패! (성공률 80%)`
- 재료 부족: `❌ 재료 부족: 철 5개 필요 (현재 3개)`

### 산출물
- ✓ v9.08.html 생성 (consumeMats 함수 + 12개 스킬 수정)
- ✓ 개발일지 업데이트 (2026-07-25 항목 추가)
- ⏳ DEVREF-E 2-5 상태 변경 (별도 수행)

---

## 2026-07-25 — v9.09 카드 효과 시스템 Phase 3 완성 (1-7)

### 개요
**미구현 카드 76개의 effect 필드를 모두 정의하여 카드 시스템 완성**

핵심: v9.04~v9.05 구축한 6가지 effect 타입 시스템을 모든 카드에 적용
- 스탯 보너스, 종족/속성 보너스, 상태효과, HP/SP 흡수, 특수 효과

### 구현 내용

#### 1. effect 타입별 분류

| 타입 | 처리 위치 | 카드 수 | 예시 |
|---|---|---|---|
| **stat** (스탯) | calcStats() | 39개 | 절망의 신 모로크: STR+3, INT+3, DEX+3 |
| **raceBonus** (종족) | processTurn() | 34개 | 현신(골렘형): 골렘형에게 120% 데미지 |
| **derived** (파생) | calcStats() | 1개 | 은총받은 자: HIT+15 |
| **special** (특수) | 맞춤 로직 | 2개 | 이미르의 잔해: 드롭률 증가 |
| **총합** | | **76개** | 모든 미구현 카드 완성 |

#### 2. 주요 카드 효과 (샘플)

**고타입 카드들**
- 절망의 신 모로크 카드: STR+3, INT+3, DEX+3 → stat
- 현신(골렘형) 카드: 골렘형에게 +20% 데미지 → raceBonus
- 텐드릴리온 카드: 식물형에게 +25% 데미지 → raceBonus
- 이미르의 잔해 카드: 드롭률 +20% → special
- 상처받은 모로크 카드: EXP 획득 +20% → special

**무기 카드 (근거리/원거리)**
- 프리저 카드: STR+1 → stat
- 캐럿 카드: INT+1 → stat
- 스팅 카드: AGI+1 → stat
- 마리 카드: LUK+1, CRIT+1 → stat (복합)
- 스켈레톤 워커: STR+2 → stat

**방어구 카드 (갑옷/투구/신발/방패)**
- 페롤 카드: VIT+1, HP+100 → stat (복합)
- 루나틱 카드: LUK+1, CRIT+1 → stat (복합)
- 드롭스 카드: DEX+1, HIT+3 → stat (복합)
- 파브르 카드: VIT+1, HP+100 → stat (복합)
- 호넷 카드: STR+1, ATK+3 → stat (복합)

**보스 카드들**
- 보아 카드: 보스에게 +15% 데미지 → raceBonus (type: boss)
- 바이올렛 카드: 보스/미니보스에게 +10% → raceBonus (type: boss)
- 드라이어드 카드: 보스/미니보스에게 +10% → raceBonus (type: boss)

#### 3. 효과 구현 스키마 (예시)

**stat 타입 (39개)**
```json
"현신(골렘형) 카드": {
  "effect": "stat",
  "stats": { "str": 1, "int": 1, "dex": 1 }
}
```

**raceBonus 타입 (34개)**
```json
"현신(골렘형) 카드": {
  "effect": "raceBonus",
  "race": "골렘",
  "dmgMult": 1.2
}
```

**derived 타입 (1개)**
```json
"은총받은 자 카드": {
  "effect": "derived",
  "bonus": { "hit": 15 }
}
```

**special 타입 (2개)**
```json
"이미르의 잔해 카드": {
  "effect": "special",
  "type": "increaseDropRate"
}
```

#### 4. 계산 로직 (기존 유지)

- **calcStats()** (라인 6407~): stat, derived effect 계산 ✓
- **processTurn()** (라인 8019~): raceBonus, seProc 처리 ✓
- **특수 효과**: 이미르/상처받은 모로크 등은 아이템 드롭/EXP 계산 시 별도 처리

### 산출물
- ✓ v9.09.html 생성 (76개 카드 effect 필드 완성)
- ✓ 미구현 카드 0개 (완전 해결)
- ✓ 개발일지 업데이트
- ⏳ DEVREF-E 1-7 상태 변경 (별도 수행)

---

## 2026-07-26 — v9.10 능력치 상세 분석 Tooltip 구현

### 개요
스테이터스 UI의 모든 능력치에 마우스 호버 시 상세 분석 정보를 표시하는 Tooltip 시스템 구현. 각 능력치가 어디서 올라갔는지(기본값, 직업보너스, 장비/카드/버프)를 명확하게 표시.

### 구현 내용

#### 1. 능력치 분석 Breakdown 함수 15개 추가
- `buildStatBreakdown()` — 기본 6개 스탯(STR, AGI, VIT, INT, DEX, LUK)
- `buildAtkBreakdown()` — 공격력(공식: STR/DEX + 무기ATK + 제련보너스)
- `buildMatkBreakdown()` — 마법공격력(공식: INT + (INT/5)²)
- `buildDefBreakdown()` — 방어력(VIT 기반)
- `buildMdefBreakdown()` — 마법방어(INT 기반)
- `buildHitBreakdown()` — 명중(Base LV + DEX + LUK)
- `buildFleeBreakdown()` — 회피(Base LV + AGI + LUK)
- `buildCritBreakdown()` — 크리티컬(LUK × 0.3)
- `buildAspdBreakdown()` — 공격속도(무기기본딜레이 - AGI감소)
- `buildMaxHpBreakdown()` — 최대HP(직업기반공식)
- `buildMaxSpBreakdown()` — 최대SP(직업기반공식)

#### 2. calcStats() 함수 수정
- 반환 객체에 `statBreakdowns` 필드 추가
- 각 능력치별로 breakdown 객체 생성 및 저장
- 게임 로직 영향 최소화 (기존 반환값 유지)

#### 3. UI/UX 개선
- renderChar() 내 능력치 표시에 `data-stat` 속성 추가 (STR, AGI, VIT, INT, DEX, LUK, ATK, MATK, DEF, FLEE, HIT, ASPD)
- 호버 가능 요소 스타일: 포인터 커서 + 파란색 글자
- 모바일 터치 지원 (touchstart/touchend)

#### 4. Tooltip 렌더링 함수 3개 추가
- `showStatTooltip(e)` — Tooltip 생성 및 표시 (화면 내 위치 자동 조정)
- `hideStatTooltip()` — Tooltip 제거
- `formatBreakdownTooltip(breakdown)` — breakdown 객체를 HTML로 변환
- `initStatTooltips()` — 게임 로드 후 모든 능력치 요소에 이벤트 리스너 등록

#### 5. CSS 스타일 추가 (14개 클래스)
```css
.stat-tooltip              /* 기본 Tooltip 스타일: 검은 배경, 금색 테두리 */
.stat-tooltip-header       /* 제목: 능력치명 + 최종값 */
.stat-tooltip-section      /* 섹션 구분: 기본값, 직업보너스, 장비/버프 등 */
.stat-tooltip-line         /* 한 줄: 항목명 + 수치 */
.stat-tooltip-value        /* 수치 표시: 초록색 */
.stat-tooltip-formula      /* 계산 공식: 파란색 */
```

Tooltip 위치 자동 조정:
- 화면 우측 끝: 왼쪽으로 표시
- 화면 아래쪽: 위로 조정
- 다크모드 지원

### 테스트 항목 (상태: ⏳ 필드 테스트 필요)
- [ ] 모든 능력치 호버 시 Tooltip 정상 표시
- [ ] 장비 착용/벗기 시 수치 변화 반영
- [ ] 버프 ON/OFF 시 수치 변화 반영
- [ ] 카드 장착/해제 시 수치 변화 반영
- [ ] 모바일 터치 작동 확인
- [ ] Tooltip 위치 자동 조정 확인

### 산출물
- ✓ v9.10.html 생성 (능력치 Tooltip 시스템 완성)
- ✓ 개발일지 업데이트
- ⏳ 필드 테스트 및 버그 수정

-----

## 2026-08-07 — v9.11 (스탯 원작화 · 전승 퀘스트 · 전직 트리맵 · 드롭/무게 복구)

- 한 것: ①스탯 누진비용 원작화(비용 `floor((n-1)/10)+2`, 지급 `floor(lv/5)+3`, 초기 48/전승 100) ②전승 퀘스트 원작 3단계 체인(메테우스 실페→이미르의 책→발키리) ③상단바 전직 트리맵 신설 ④드롭명 별칭 247 + 무게 2,745종 주입 → **드롭 장비 57종→286종(5배)**, 유령 드롭 1,748건→0
- 함께 고친 기존 결함 A~L 12종. 특히 `advanceJobQuestStep` **미정의**로 전직퀘 전반이 파손 상태였고, job3 13종은 `reqJob`이 비전승 2차라 **전승해도 2차 직업 도달 불가**였음
- 남은 것 / 다음 세션 최우선: 무게가 처음으로 실제 작동 → **체감 밸런스 확인**(90% ASPD 패널티·전승 무게 500). 드롭 확률 중앙값 0.15%라 이름을 고쳐도 여전히 박함 → DEVREF-E 신규 항목 참조
- 오늘 밟은 함정:
  - **브라우저 캐시 2회** — `?v=` 를 바꿔도 구버전이 서빙돼 코드가 멀쩡한데 20분 헛짚음. 검증 전 신규 심볼 `typeof` 확인을 선행할 것
  - **JSON 서식 파괴** — db-items/db-monsters를 한 줄로 압축해 저장. 사람이 직접 편집하는 파일이라 원본 서식(items=indent2, monsters=몬스터당 1줄) 보존 필수
  - **계획서에 적어놓고 안 지킴** — "다크모드는 CSS 변수 쓸 것"이라 써두고 하드코딩해 노드 8개가 흰 박스로 남음
  - **버전 백업 라벨 오염** — 최종 수정본을 `versions/v9.10.html`로 복사해 라벨이 거짓이 됨. 중복 확인 후 제거

-----

## 2026-08-11 — v9.12 (스텁 아이템 실데이터화 · 번역 중복 통합 · 원작 소모품 효과 연동)

- **DEVREF-E I-2 착수**: 스텁 528종 중 드롭 빈도 상위 150종을 rAthena Pre-Renewal `item_db`(공식 원작 데이터)와 KR↔AegisName 매핑으로 대조해 143종 실데이터화(`buy`/`sell`/`weight`, `weightSrc:"official"` 신규). 스텁이 차지하던 드롭 물량 비중 **71.7%→12.8%**. 스텁 528→376종
- **번역 파이프라인 중복 9건 발견·통합**: 위 작업 중 `엘루니움`(중), `엘누니움`(제작 레시피), `엘루니움 스톤`, `오리데콘`이 이미 구현된 `에르늄`/`에르늄원석`/`오리데오콘`(제련 핵심 재료 2종)의 번역중복이었음을 발견. 같은 패턴으로 `낡은 카드앨범`→`낡은 카드첩`, `블루 포션`→`파란포션`, `화이트/블루/그린 허브`→`하얀/파란/초록허브`, `불의 화살`→`불화살`도 발견해 전부 병합(스텁 정의 삭제 + 드롭테이블 키 치환, 레이트 합산 충돌 0건)
- **원작 소모품 11종 효과 연동**: Type=Healing인데 `잡템`으로 방치되어 "사용"해도 아무 효과 없이 사라지던 아이템들(빨간허브·옐로허브·고기·사탕·캔디스트라이퍼·꿀·마스텔라의열매·딸기·위그드라실의열매·씨앗·몬스터사료) — 원작 스크립트 수치(`itemheal`/`percentheal`) 그대로 `heal`/`spHeal`/`effect` 필드 주입, `type:"소모품"` 승격. `useItem()`에 `effect:'half'` 신규 3줄 추가(위그드라실의 씨앗 전용 50% 회복 — 기존 `'full'`은 100%뿐이었음)
- 남은 것 / 다음 세션: 스텁 잔여 376종(드롭 빈도 꼬리, 상위 200종까진 물량 90.6% 커버). 새 스텁 채울 때는 **먼저 번역중복 여부 확인 필수**(위 9건과 같은 패턴 재발 가능, DEVREF-B 체크리스트 참조)
- 오늘 밟은 함정:
  - **weightSrc 태그 없이 rAthena 값 그대로 못 씀** — 프로젝트 자체 `csv`(실측)와 외부 소스가 섞이면 출처 추적이 무너짐 → `official` 태그 신설로 구분
  - **Type=Healing 승격 시 `effect` 없이 `type:"소모품"`만 바꾸면 무음 소모 버그** — `useItem()`이 `heal`/`spHeal`/`effect` 미매치 시 아이템만 소모하고 아무 로그도 안 남김. 승격 전 반드시 원작 스크립트 수치 확보 후 필드까지 같이 채울 것
- **버전**: `룬미드가츠_v9.11.html` → `versions/`로 스냅샷 이동, `룬미드가츠_v9.12.html` 신규 (CLAUDE.md·DEVREF-INDEX·DEVREF-B·DEVREF-E 동시 갱신)

-----

## 2026-08-12 — v9.12 (I-1 장비드롭 복구 · I-5 이름매핑 · rAthena 스탯 실데이터화) [in-place]

- **진단 정정**: I-1 "드롭 박함"은 밸런싱 아닌 **데이터 결손**. 몬스터 490중 179가 수기 임시엔트리(`{잡템,카드}`, 판별=`atkDelay/race/elementLv/mdef` 필드 유무). 스폰 105마리가 Lv60~99에 71%
- **I-5 완료**: 몬스터 한글명↔AegisName 매핑표 `데이터베이스\몬스터_이름매핑.csv`(확정75/검토21/보류9). 확률+스탯 시그니처 자동 → RO지식 별칭사전 → 검증 리졸버(known 256쌍에 먼저 돌려 정밀도 96% 확인 후 적용)
- **I-1 주입**: 스폰 96마리에 원작 드롭 주입. 장비0 몬스터 266→196. **검증: 196/490=40%는 원작 충실**(Pre-Re CSV 자체 38.7%). P5로 명백버그 2건(포이즌스포아 유령 모자·베르제브브 공백 MVP)
- **rAthena 실데이터화**: `db/pre-re/item_db_*.yml` curl → AegisName↔Id↔스탯. 장비 43 + 재료 91 = 134종을 원작 스탯(atk/def/wType/weight/slots/buy/sell)으로 등재. 전 490몬스터 유령드롭 0
- 남은 것 / 다음 세션: **kRO 원작 한글명** — `_krPending:true` 134종 대기. Divine-Pride API 키(무료) 확보 시 `_aegis`→Id→ko-KR 배치, B안(krName 오버레이·개명 아님)
- 오늘 밟은 함정:
  - **퍼지 이름매칭 오탐** — Celestial_Robe→성천도끼(도끼), Cursed_Hand→모로크현신1. score만으론 재료/장비 안 갈림 → **안정적 Id로 조인**하거나 타입 게이팅+검증셋 필수
  - **CSV 몬스터 변종 다수** — Beelzebub는 MVP행(1874, 풀드롭)과 non-MVP행(1873, 공백) 분리. drops>0+레벨근접으로 행 선택 안 하면 공백행 매칭
  - **kRO 한글 벌크소스 없음** — rAthena/zackdreaver/ratemyserver 전부 영문. 순정 한글=Divine-Pride API(키)뿐(401 확인)
- **버전**: in-place 편집(유저 버전bump 미요청). 백업 scratchpad predrop/preP5/preStats/preMat 4단

-----

## 2026-08-12 — v9.13 (기술 부채 청소 · DEVREF-E 4순위)

DEVREF-E 「4순위 기술 부채」의 살아있는 항목을 청소. **기능 무변경, 죽은코드·중복·디버그잔재 제거 + 안전한 중복호출 정리만.** v8 기준 줄번호는 전부 v9.12 실측으로 재확인 후 작업.

- **B1 디버그 console.log 14줄 제거** — `initLobby`/`createCharacter`에 `new Error().stack` 덤프·모달 computed-style 덤프·`tryMove loaded` 로드마커 등. 게임 로직 무관 잔재
- **B2 `window.onerror` 중복정의 제거** — 두 벌 중 뒤엣것이 앞엣것을 덮어써 앞(치명오류 메시지 버전)은 死코드. 뒤 1벌만 유지
- **B3 `flash()` no-op 제거** — 본문이 "번쩍임 효과 모두 제거" 주석뿐인 빈 함수. 유일 호출부(제련 성공 로그)와 정의 함께 삭제
- **B4 BUFF_DEFS `safety` "(구형)" 유령키 제거** — 실사용은 `safetyWall`(생성·소모 3곳). `safety` 키 읽는 곳 0건 확인 후 삭제
- **B5 프리셋 `wType:"검"` → `"한손검"`** — 전사 지망 프리셋의 무효 wType. 장착무기 소드의 실제 wType(`한손검`)로 정정. (calcStats가 장착무기에서 weaponType 재설정하므로 vestigial이나 무효값 제거)
- **B6 `tickStatusEffects()` 삭제** — DEVREF엔 "빈 정의"라 적혔으나 실물은 정상 구현체(SE턴 차감+만료). **단 호출 0회**, 실제 SE 차감은 processTurn 내 `nSe` 재구성 루프(카운트있음/없음 2분기)가 담당. 연결하면 이중차감 위험 → 삭제가 정답. 死코드 제거라 런타임 무영향(구조적 보장)
- **calcStats 2a**: 전직추천 툴팁의 `reasons` 객체가 `calcStats()`를 12회 호출(실사용은 `reasons[rec]` 1개뿐, 11회 순수 낭비) → `let cs=calcStats()` 1회로 hoist. 2b(턴스코프 캐시)는 **미착수** — calcStats가 `p.weaponType`/`weaponElement`를 변이하는 비순수 함수라 전역 메모이즈 위험, processTurn 700줄 리팩토링과 묶어 별도 진행 권고
- **검증**: 로컬 http 서버(scratchpad 사본) 로드 → JS 파싱 무에러(calcStats·processTurn·createCharacter·equipItem·initLobby 전부 `typeof function`, `flash`·`tickStatusEffects` `undefined`), `window.onerror` 정확히 1개, console.log 0, `wType:"검"` 0. 로비·캐릭생성 모달·전사 지망 프리셋 정상 렌더, onerror 미발동
- **processTurn 분해 슬라이스1** — 752줄 단일함수에서 **자립 리프 단계 5종을 헬퍼로 추출**(동작 불변): `procAutoBerserk`(오토버서크) · `procNaturalRegen`(실시간 자연회복) · `procAggressiveSpawn`(공격적 추가스폰) · `procResummonSlaves`(MVP 슬레이브 보충) · `procPlayerStatusTick`(원령무사자해+SE 틱다운). 코드는 토큰 그대로 이관(JS 들여쓰기 무관). processTurn ~752→~650줄
  - **선정 기준**: 함수 중간 `return`(battles 비면 즉시탈출, 5곳)이 없고 로컬변수 출력이 없는 단계만. 스킬시전·처치보상 코어(C6, return 낀 구간)와 몬스터턴(H, 159줄)은 **미착수**
  - **검증**: 전사 생성→몬스터 스폰→processTurn 40틱 **무예외**(18처치·EXP·회복·피격 정상, wType=한손검). SE 틱 정밀검증: testbuff 3→2→1→만료, safetyWall 5→4→3→2→1, bleeding 4→3→2→1→만료 = **정확히 턴당 1차감·정상만료, 이중차감 0**(원본과 동작 동일 입증)
  - 함정: **159줄 대형블록 공백-정확 매칭 리스크** — 후행공백 있는 줄(전투코어) 다수라 Edit 통짜 매칭 실패 위험. 몬스터턴 추출은 경계만 나눠 진행(슬라이스2에서 해소)
- **processTurn 분해 슬라이스2** — 몬스터 턴(159줄)을 `runMonsterTurn(p, s)` 함수로 캡슐화(`dd` 반환, 동작 불변). **핵심 기법: 159줄 통짜 매칭 대신 경계 2군데만 편집** — 오프닝(주석+`let fl`+`for`)에 `let dd=runMonsterTurn(p,s); function runMonsterTurn(p,s){` 삽입, 클로징(`for` 닫힘+`updateUI` 사이)에 `return dd; }` 삽입. 본문 159줄 무손상 → 후행공백 리스크 회피. 메인 흐름은 `let dd = runMonsterTurn(p,s)` 한 줄로 읽힘
  - **현재 중첩 함수**(processTurn 내부, 매 틱 클로저 재생성 — 비용 무시가능). p,s만 파라미터+나머지 전역이라 스테일 클로저 없음. 최상위 승격은 통짜 이관 필요(향후)
  - **검증**: ①저레벨 40틱 생존전투 17처치 무예외 ②**사망 경로**: HP 강제저하+강한몹 → `dd`가 runMonsterTurn→processTurn 사망처리로 정확 전파(예외0, HP=1, 마을귀환 프론테라, G.battles=0). 몬스터 SE/보스스킬/방어SE/반사 다수 히트 무예외
- **processTurn 분해 슬라이스3a** — 스킬 자동선택 로직(쿨다운틱·티어큐 빌드·healPick·pickFromTier·buffPick·fallback, ~130줄)을 `pickPlayerSkill(p, s)`로 캡슐화(`pick` 반환, **return 없음이라 안전**). 슬라이스2와 같은 경계-2편집 기법. 선정 근거: `mySkillNames`·`hotbarSkillSet`·`_NC`·`healPick`·`allActSkills` 전부 선택구간 내에서만 쓰임(캐스트 이후 누출 0, grep 확인). 메인흐름은 `let pick = pickPlayerSkill(p,s)` 한 줄
  - **검증**: ①힐스킬 부여+HP낮춤 → healPick 경로 시전(SP 200→21, 40틱) ②데미지스킬(네이팜비트 rank1)+HP풀유지 → pickFromTier 경로 시전·처치(50틱), 총 90틱+ 무예외, JS에러 0
- 남은 것 / 다음 세션: **processTurn 슬라이스3b(캐스트/처치 코어) — 최난이도**. 스킬시전+평타+처치보상 구간에 함수 중간 `return` 3곳(battles 비면 즉시탈출) + 처치보상 이중처리 제어흐름. return→`return true` 신호값 전환 + 호출부 `if(exec(...))return;` 필요. 실패모드=조용한 EXP/처치 버그(육안검증 회피)라 **전용 패스 권장**. + runMonsterTurn/pickPlayerSkill 최상위 승격(선택) + calcStats 2b + 장비 diff 양손무기 방패(기능버그) + 아이폰 QuickLook. 2·3순위 밸런스(2-3·2-6·2-7)도 대기
- 오늘 밟은 함정:
  - **file:// 브라우저 로드 차단** — iCloud 경로(`~`·한글) + 스크래치패드 ASCII 경로 둘 다 `navigate`/`preview_start` file:// 거부. 정적 http 서버(`python -m http.server`, launch.json config)로 우회. 단일 HTML 검증 표준 경로로 기록
  - **死코드 삭제 = 무회귀 보장** — B6처럼 호출 0회 확정된 함수는 삭제가 연결보다 안전(연결 시 기존 로직과 이중실행). 삭제 전 호출부 grep 0건 + 대체 로직 위치 확인이 근거

-----

## 2026-08-13 — DEVREF-E Backlog 재구조화 (문서 정리, 코드 변경 없음)

DEVREF-E가 완료 이력·상세 스펙·기술 조사까지 뒤섞인 "잡동사니 문서"가 되어, **현재 할 일만
보여주는 Backlog 전용 문서**로 재편했다. 삭제는 없음 — 전부 소유 문서를 옮겼다.

- **완료 이력 전량** (우선순위 목록에서 이동 표 + 기존 완료 이력 표, 총 26행) → 본 문서 위
  「완료 이력 상세 표」섹션. 분리 신설: 2-1을 ①노점비율/②스틸코인(완료)/③매머나이트로 분리,
  2-2를 자연회복분리(완료)/스킬쿨·화살SE(잔여)로 분리 — 완료분만 표에 추가, 잔여는 신규
  Backlog로 이관
- **스킬 퀘스트 10개·체인 퀘스트 14개 상세 표 + 스킬 시스템 현황** → `DEVREF-D 퀘스트 스킬
  전투.md`
- **dropSlots 필드 배경** → `DEVREF-B DB스키마.md`
- **ASPD 연동 타이머(자연회복/스킬쿨/화살SE) 배경** → `DEVREF-G 전투 상태이상.md` (신설 섹션)
- **processTurn/calcStats 리팩토링 배경**은 이미 `DEVREF-A 구조 코드체계.md`에 있던 것을 재확인,
  DEVREF-E 쪽 중복 서술만 제거하고 상호 링크 추가
- **장비 diff 양손무기 한계**는 이미 `DEVREF-F 에디터 UI.md`에 있던 것에 링크만 추가
- 알베르타↔페이욘 숲 편도연결 버그: 지난 이관에서 "미해결"로 잘못 표기했던 항목 — 위
  2026-07-25 v9.07 섹션(§6 데이터 정합성 검증)과 DEVREF-INDEX 패치이력에 "핫픽스 확인됨"으로
  이미 기록되어 있음을 재확인, Backlog에서 제외
- 신규 Backlog ID 체계(P0~P5 + 외부의존/보류)로 전환, 구 ID는 각 항목에 "기존 ID"로 보존.
  상세는 `DEVREF-E 미구현목록.md` 본문 참조

## 2026-08-25 — v9.14 (P2-02 무게 구간별 패널티)

DEVREF-E `P2-02 무게 패널티 확장`. v9.11에서 무게 시스템이 처음 실작동했지만 패널티가 90%
ASPD 하나뿐이라 "무겁게 들고 다녀도 사실상 손해가 없는" 상태였다. 원작 Pre-Re 구간을 방치형에
맞게 조정해 3구간으로 확장.

| 구간 | 패널티 | 구현 위치 |
|---|---|---|
| 50% 초과 | HP/SP 자연회복 정지 | `procNaturalRegen()` 선두 게이트 |
| 70% 초과 | **자동** 전투 스킬 사용 불가 (평타만) | `pickPlayerSkill()` 선두 게이트 → `null` 반환 |
| 90% 초과 | ASPD 딜레이 ×1.3 (기존) | `calcStats()` |

- **임계값은 calcStats가 단독 소유** — `weightNoRegen`(>0.5)·`weightNoAutoSkill`(>0.7)을 계산해
  결과 객체로 노출하고, 소비처는 `s.weightNoRegen` / `s.weightNoAutoSkill`만 읽는다. 임계 숫자가
  여러 함수에 흩어지지 않게 한 곳 소유 (이 프로젝트 1번 사고원인 = 같은 로직 복붙)
- **원작과 의도적으로 다르게 간 지점**: 원작은 70%에서 스킬 사용 자체가 막히지만, 방치형이라
  **자동 로테이션만 차단하고 수동 시전(`useSkill`)은 그대로 허용**했다. DEVREF-E 항목의 "방치형
  특성상 70%→자동스킬 불가로 조정 가능" 지침을 따른 것. 수동까지 막으려면 `useSkill()`의 침묵
  상태이상 체크 옆에 같은 플래그 분기를 넣으면 된다
- **자연회복 게이트가 타임스탬프를 계속 갱신하는 이유**: 그냥 `return`만 하면 과적재로 보낸
  시간이 `now - lastHpRegenTime`에 그대로 쌓여, 짐을 던진 순간 밀린 회복이 한꺼번에 터진다.
  게이트 안에서 `lastHpRegenTime`/`lastSpRegenTime`을 `now`로 밀어준다
- **쿨다운 차감은 게이트보다 앞** — `pickPlayerSkill`의 쿨다운 틱 차감 루프 **뒤에서** `return
  null`. 순서를 뒤집으면 과적재 동안 쿨다운이 얼어붙어, 짐을 던진 직후 전 스킬이 쿨 상태로 남는다
- **경고 로그는 30초 스로틀** (`G._wRegenWarnAt`/`G._wSkillWarnAt`). 매 틱 찍으면 로그창이 과적재
  메시지로 도배된다. `p`가 아니라 `G`에 둬서 세이브 스키마는 무변경 (레거시 세이브 영향 0)
- **UI**: `updateUI()`의 무게 표시 색 구간을 실제 패널티 구간(50/70/90)에 맞췄다. 기존은 50/90
  2구간이라 70~90 사이가 계속 노랑으로 보여 **자동스킬이 왜 멎었는지 화면상 알 수 없었다.**
  50% 초과부터 `⚠N%` 뱃지 + `title` 툴팁으로 현재 걸린 패널티를 나열
- **적용 범위 주의**: `procNaturalRegen`은 `processTurn`(=전투 중)에서만 호출된다. 즉 50% 게이트도
  전투 중에만 의미가 있고, 비전투 자연회복 경로는 애초에 없다. P1-02(전투 중 자연회복 정지)가
  현재 구조와 정반대로 읽히는 것도 같은 이유 — **P1-02 착수 시 이 게이트와 묶어 재설계할 것**
- **검증**: 로컬 http 서버(`python -m http.server`) 로드 → 콘솔 에러 0, 전 `<script>` 블록
  `node --check` 통과. 게이트 실동작은 페이지 안에서 직접 호출해 확인 —
  ① `procNaturalRegen`: 과적재(ratio .62) HP/SP 10→10 무회복, 정상(.30) 10→17 / 10→15 회복
  ② `pickPlayerSkill`: `weightNoAutoSkill` 시 `null` 반환 + **쿨다운은 정상 차감**(3→2, 1→만료)
  + 경고 1회만 로그. UI는 40/60/80/95% 경계값에서 색·툴팁 4구간 분기 확인
- **버전/아카이브 경위**: 패치는 `v9.13.html`에 in-place로 들어갔고, 마무리에서 `v9.14.html`로 승격(rename).
  in-place라 순수 v9.13 스냅샷이 사라졌었는데, **패치 스크립트의 (old,new) 쌍을 역적용해 복원**해
  `versions/룬미드가츠_v9.13.html`로 아카이브했다 (바이트 1,922,481 원본 일치 확인). VCS가 없어
  스냅샷이 유일한 롤백점이므로, 앞으로도 in-place 패치 후에는 역패치 복원으로 체인을 메울 것
- **문서 정리 (마무리 단계)**:
  ① 코드폴더 `CLAUDE.md` **82→46줄** — HTML 블록순서·참조파일 역할·자주 나오는 함정 5종이 전부
  DEVREF-A/C/INDEX에 원문으로 있어 **중복 강등**(원문 대조 후 삭제, 지식 손실 0). 대신 버전 절차에
  "in-place 패치된 직전 버전은 역패치로 복원한 뒤 bump" 한 줄 신설
  ② `DEVREF-B` 318줄(예산 300 초과) → 아이템 계열 전량을 **`DEVREF-H 아이템DB.md`로 분리**
  (B 194줄 / H 136줄). 「임시 몬스터 코호트 판별」은 주제가 몬스터라 B로 되돌림. 아이템 참조 11곳 재조준
  ③ L2-G 승격: [[CORE_DEVREF-B]] **#63**(앵커 치환은 매치 개수를 세고 쓴다) 신설 + #50 보강(프로브
  대상 심볼 오선정 실패모드)
- 오늘 밟은 함정:
  - **이 html은 CRLF + BOM** — 패치 스크립트를 LF 앵커로 짜서 5개 앵커 전부 0매치. 다행히
    앵커 개수 검증에서 걸려 쓰기 전에 죽었고 원본은 무손상. `newline=None`으로 읽어 정규화 →
    치환 → `utf-8-sig` + `newline='\r\n'`(CRLF)로 되쓰기로 확정. **볼트 파일 패치 = 앵커 개수 검증 →
    임시파일 → `os.replace`** (덮어쓰기 직행 금지, iCloud 잠금 시 원본 소멸)
  - **이스케이프는 편집 파이프라인의 겹마다 한 번씩 먹힌다** — 개발일지·CORE_DEVREF에 개행
    이스케이프를 *텍스트로* 쓰려다 두 번 다 진짜 CRLF가 문서에 박혔다(LF 문서에 CR 1개씩
    혼입). 셸 heredoc → 파이썬 리터럴 → 파일로 3겹이라 그렇다. `chr()` 조립이나 낱말
    풀어쓰기로 회피. 마무리 검사에 **`d.count(CR바이트) == 0`** 추가 ([[CORE_DEVREF-B]] #63)
  - **패치 검증 때 감싸는 함수 이름을 추측하지 말 것** — 무게 UI가 `renderChar`에 있는 줄 알고
    `renderChar.toString()`으로 조회했다가 "미반영"으로 오판할 뻔했다. 실제 소유자는 `updateUI()`.
    (게다가 파일 끝에서 `renderChar`를 래퍼로 재할당하고 있어 원본 소스가 안 보인다.)
    줄번호로 감싸는 함수를 먼저 확정한 뒤 조회할 것

-----

## 2026-09-12 — v9.15 (P0 3건: 마법판정 · 오프라인정렬①② · hasMvp 7/14)

- 한 것: P0-01(마법 job 코드판정을 `MAGIC_JOB_CODES`/`isMagicJobCode()` 단일소유로 교체) · P0-02①②(`getJobLvCap` 통일, 탭 숨김 시 `huntTimer` 즉시정지) · P0-03(hasMvp 오류 14건 중 원작 확인+DB실재 7건 교정: 아울바론·키엘-D-01·아트로스·크툴라눅스·이프리트·RSX-0806·고피니치) — 전부 브라우저에서 실함수 호출로 검증(processTurn 실행 로그, clearTimeout 스파이, spawnMonsters 보스만사냥 확인). v9.14→v9.15 승격, 구버전 `versions/`에 스냅샷
- 남은 것 / 다음 세션 최우선: P0-02③④(`_silentTurn` 온라인엔진 헤드리스 통합 — `P4-01`과 얽혀 설계판단 필요, 상위 모델 권장) · P0-03 잔여 7건(생체연구소4층·어비스호수심층·쿤룬·용지성·포트말라야·말랑도심해·엘디카스테스 — 원작 MVP 불분명 또는 이 게임 DB에 미존재, 사용자 확인 필요)
- 오늘 밟은 함정: 없음(기존 CRLF+BOM 앵커개수검증 규약으로 패치 5건 전부 1회 통과). 대신 신규 발견: db-maps `monsters`(일반 스폰) 배열에 MVP급 고HP 몬스터ID가 다른 맵에 잘못 섞여 있는 데이터 오염 확인(토르 화산에 Bio Lab 보스 6종, 얼음 동굴에 네크로맨서 등) — 상세 DEVREF-B, 백로그는 DEVREF-E P1-08②

-----

## 2026-09-13 — v9.16 (P0-02③④ 완료 · P0-03 9/14 · P1-08 검증 게이트 · P1-07 세이브 스키마 · P4-03 데이터 추출+빌드 · P4-01 processTurn 정리 · P4-02 종결)

- 한 것: **P0-02③④**(별도 Opus 세션) — `_silentTurn`/`_silentSpawn` 제거, 오프라인이 `huntTick()`을 그대로 N틱 호출(`runHeadlessTicks`). 온라인 vs 오프라인 diff=0을 시드 고정 6시나리오로 검증. **P0-03 9/14** — 사용자 보유 로컬 DB(`몬스터_MVP.csv`) 대조로 생체연구소4층→로드나이트 세이렌, 쿤룬→흑사왕(kRO 의역명) 추가 교정. **P1-08** — `runValidationGate()` 콘솔 함수로 5종 회귀 게이트 구현. **P1-07** — `loadCharacter`의 마이그레이션을 `migrateSave`/`normalizeSave`로 추출, `validateSave→loadPlayerData` 단일 경로 신설, `importGame`도 같은 경로를 타도록 교체. `p.saveVersion` 신설. **P4-03** — `db-items`/`db-monsters`/`db-maps`/`db-npcs`를 `source/data/*.json`으로 분리, `source/template.html`(14,753줄, 69% 감소) + `build.py`로 재조립. `룬미드가츠_v9.16.html`은 이제 빌드 산출물(직접 편집 금지). **P4-01** — processTurn의 복붙 코드만 정리(`return` 3곳은 그대로 두고 `_endTurnIfBattlesEmpty()`/`awardKillReward()`로 중복 통합, AOE 경로는 레벨업 타이밍이 달라 의도적 미포함). **P4-02** — 조사 후 "하지 않기로" 확정. `updateUI()` 내부 진짜 중복(2a 누락분) 1건만 고치고, 전역 dirty-flag 캐시는 위험(calcStats 530줄 의존지점 누락 시 조용한 오계산) 대비 효용(이미 30,000틱 570ms로 병목 아님 확인) 없음 판단으로 미착수 확정. **DEVREF 줄번호 전수 감사** — P4-03/P4-01로 무효해진 줄번호 인용을 DEVREF-A/E에서 전부 재확인·갱신. v9.15→v9.16 승격, 구버전 스냅샷
- 남은 것 / 다음 세션 최우선: P0-03 잔여 5건(어비스호수심층·용지성·포트말라야·말랑도심해·엘디카스테스 — 리뉴얼 이후 맵이라 손대지 않기로 확정) · P4 카테고리 전부 종결, 다음은 P1-07 이후 남은 P2~P5 백로그 또는 코드 6분할 재판단(template.html이 이미 14,753줄이라 시급성 낮아짐)
- 오늘 밟은 함정: ① **동시 세션 충돌** — 같은 1.9MB 파일을 두 세션이 동시 read-modify-write, 상대 세션의 커밋 하나가 내 P0-03 수정 1줄을 조용히 덮어썼다가(에러 없음) 상대가 자체 diff로 잡아 복구. 이후 git 인덱스 직접 조작(hash-object+update-index)으로 워킹트리 안 건드리고 격리 커밋하는 방식으로 전환. ② kRO 몬스터명 의역(흑사왕=Evil Snake Lord) 놓쳐 음역 검색만으론 못 찾음(사용자 확인으로 해결). ③ P1-08 게이트③이 자기 자신을 설명하는 주석 속 예시 리터럴에 자기지시적으로 매칭 — 코멘트에서 리터럴 제거로 수정. ④ **P1-08 게이트④⑤를 실행해보지 않고 커밋** — P1-07 작업 중 실제 캐릭터로 돌려보고서야 둘 다 비교 대상이 잘못됐다는 게 드러남(온라인 맨 루프는 사망복구 스캐폴딩 없음, round-trip은 raw와 1회 통과분을 비교). 새 게이트는 정상 케이스 PASS를 실측하기 전엔 커밋하지 않기로. ⑤ **db-items 서식이 균일하지 않음** — DEVREF가 "items=indent=2"라 적어놨지만 실제 파일은 일부만 그렇고 나머지는 항목당 1줄이라 첫 시도(항목당 1줄 재조립)가 원본과 안 맞았다. 바이트 일치가 아니라 **`json.loads` 딥 비교(값 단위)**로 기준을 바꿔서 해결 — db-items는 사람이 여러 세션에 걸쳐 손으로 편집해온 파일이라 서식이 애초에 안 고르다. ⑥ **P4-01 전/후 비교가 처음엔 틀리게 나옴** — 같은 시드로 두 빌드를 돌렸는데 결과가 달랐다. 원인은 리팩토링이 아니라 테스트 방법 — 이전 테스트에서 남은 잔여 상태(누적 존kill·쿨다운 등)가 섞인 "쓰던" 탭과 방금 새로고침한 "깨끗한" 탭을 비교하고 있었다. 두 탭 다 완전히 새로고침한 뒤 재비교하니 완전 일치 — **before/after 비교는 반드시 양쪽 다 갓 새로고침한 상태에서** 할 것

-----

## 2026-09-20 — P2-10 부분완료 (문구·중복ID·死코드·조사처리) · ⚠️ 빌드 파이프라인 단절 발견

- 한 것: P2-10 6개 중 4개. **`source/template.html` 직접 수정, 빌드 전.**
  - 캐릭터 생성 안내문 "스탯 포인트 24개" → "48개"(`totalStatPointsAt` 실제값과 일치)
  - `id="stat-matk"` 중복 2건 중 죽은 쪽(플레이스홀더 "1", 갱신 안 되던 줄) 삭제 — 살아있는 쪽은 `updateUI()`가 `innerHTML`로 갱신하는 줄(getElementById가 DOM 순서상 먼저 걸리는 줄)로 확인 후 나머지만 제거
  - `log()` 중복 정의 중 死코드(앞엣것, `G.gameLog` 적재만 하고 소비자 0건) 삭제, 활성본(DOM 렌더 담당) 유지
  - **조사(이/가·을/를) 처리 통일** — 원래 백로그엔 "모험가이(가)" 1곳으로 적혀 있었으나 실제로는 전투 로그 등 **16곳**에서 `이(가)`/`을(를)`을 리터럴 문자열로 그대로 찍고 있었음(받침 무관 항상 같은 텍스트 출력). 유니코드 완성형 한글 코드포인트로 받침 유무를 판정하는 `josa(word, pair)` 헬퍼를 `log()` 함수 옆에 신설하고 16곳 전부 교체. 비-한글/빈 문자열은 받침없음(뒤쪽 조사)으로 안전 폴백
  - 검증: 실제 JS `<script>` 블록 9개(데이터 전용 JSON 블록 제외) 전부 `node --check` 통과
  - 미착수 2건(HTML 구조 정리, 개발도구 리본 이동)은 이번 세션 범위 밖 — 다음에
- **⚠️ 빌드 불가 확인 — 다음 세션 필독**: 위 수정을 `룬미드가츠_v9.16.html`에 반영하려 `python build.py` 실행 시도했으나 실패.
  - `source/data/*.json`(db-monsters/db-maps/db-npcs/db-items) 이 **이 머신(D:\Vault_JROC)에 아예 없음** — `source/` 안에 `template.html`만 존재
  - `.git`도 손상 확인: `HEAD`·`config`·`index`는 정상 읽히는데 **`.git/objects/`·`.git/refs/`가 없고, 그 안에 있어야 할 해시-접두 디렉토리(03,04,07…)가 `.git/` 바로 밑에 노출**되어 있음 → `git status`/`rev-parse`가 "not a git repository"로 실패. `.git` 내 대부분 항목 mtime이 **2026-09-16 16:26**로 동일 — 그 시점에 뭔가(동기화 도구 추정) `.git` 트리 구조를 깬 것으로 보임
  - **아직 안전한 것**: `source/template.html`(이번 수정 반영됨, 891,769 bytes, 정상) · 기존 `룬미드가츠_v9.16.html`(빌드 산출물, 이번 수정 전 상태로 그대로 있음, 손상 없음)
  - **하지 않은 것(의도적)**: `source/data` 재생성, `.git init`/`git fsck` 등 복구 시도 — 원인(동기화 꼬임 추정)을 모르는 채 이 폴더에 쓰기 작업을 하면 다른 머신에 있는 진본을 덮어쓸 위험 판단, 손대지 않음
  - **다음 세션 필수 확인**: 이 프로젝트를 최근에 만진 다른 머신(또는 클라우드 동기화 상태)에서 `source/data/*.json` + 정상 `.git`이 살아있는지 대조 후 병합. 그 전까지 이 폴더에서 `git`/`build.py` 실행 금지(추가 손상 위험)
- **✅ 같은 세션에서 후속 해결**: 마이그레이션 전 정본이 `C:\Users\ADD-06\iCloudDrive\iCloud~md~obsidian\Vault_JROC_icloud\02_Dev\App\Text_Rag`에 그대로 남아있었음(9/16 이전 삭제 안 함, [[project-vault-jroc-d-drive-migration]]). 대조 결과:
  - `source/data/*.json` 4개는 이 iCloud 사본에 온전히 살아있어 **D:로 복사 완료**. `build.py` 재실행 성공, 새 `룬미드가츠_v9.16.html`이 재실행 직전 산출물과 정확히 오늘 P2-10 수정분(55줄)만큼만 다름을 `diff`로 확인 — 데이터·빌드 파이프라인 완전 정상화
  - **git 이력은 양쪽 다 죽어있어 복구 불가로 확정**: iCloud 사본도 `.git/objects` loose 0개·pack 0개(`bad object HEAD`) — 이 프로젝트 DEVREF-E가 이미 알던 "iCloud 동기화 폴더 안 git = 조용한 디하이드레이트 누락" 문제가 실제로 터진 사례. D: 사본은 9/16 마이그레이션 당시 이미 손상된 iCloud `.git`을 그대로 복사해오며 `objects`/`refs` 폴더 자체가 사라지고 빈 해시디렉터리만 `.git/` 바로 밑에 남음. `D:\backup\Vault_JROC_260826`·pack/bundle 파일 전역 검색도 전부 무소득
  - **사용자 확인 후 조치**: D:의 죽은 `.git`(내용물 0개) 삭제 → `git init -b main` → 현재 파일 전체(70개)를 새 베이스라인 1커밋(`0e94250`)으로 재기록. 9/12~9/13 커밋 이력(P0~P4 각 단계별 커밋)은 메시지·diff 단위로는 소실, 단 그 시점 이후의 실제 파일 상태는 전부 살아있어 **코드·데이터 손실은 없음**
  - **교훈**: git 저장소를 iCloud/OneDrive 등 실시간 동기화 폴더 안에 두면 대량의 작은 loose object 파일이 디하이드레이트·동기화 충돌로 조용히 증발할 수 있다. 로컬 전용 드라이브(D:)에서도 "그 iCloud 저장소를 복사해온 것"이면 손상을 그대로 물려받는다 — 마이그레이션 시 `git fsck`로 이관 직후 무결성 확인을 새 규약으로 남길 것
- **이어서 P2-10 나머지 2개 완료** (빌드 정상화 후, 같은 세션):
  - **HTML 구조** — `<title>` 바로 뒤에 있던 조기 `</head><body>`를 삭제하고 `<style>` 블록(675줄)을 `<head>` 안으로 편입. `</style>` 뒤의 `</head><body>` 하나만 남아 표준 head/body 1쌍 구조가 됨(전엔 브라우저가 암묵 복구해서 실행만 됐던 상태)
  - **개발도구 노출** — `에디터`/`DB뷰어`/`몬스터`/`밸런스` 4개 버튼을 `<span id="dev-tools-group" style="display:none">`로 묶고, 앞에 `🛠 개발자` 토글 버튼 신설. `toggleDevTools()`가 표시 여부를 뒤집고 `localStorage['rmc_devtools']`에 저장, 페이지 로드 시 다크모드(`rmc_theme`)와 같은 IIFE 패턴으로 복원. 기본값은 숨김(off)
  - 검증: JS `<script>` 블록 9개 전부 `node --check` 통과, `build.py` 재실행 후 diff 19줄 = 오늘 이 2건 수정분과 정확히 일치
  - P2-10 6/6 전부 완료. DEVREF-E는 완료 포인터로 축약, P2 잔여 건수 9→8

-----

## 2026-09-20 (계속) — P1-03 dropSlots 완료 + parseItem 미적용 8곳 교정

- 한 것: **핵심 수정은 1줄** — `rollDrops()`의 `it.dropSlots ? ... : k`를 `it.slots>0 ? ... : k`로 교체. 새 필드를 만들 필요가 없었다 — `slots`(무/유슬롯 쌍을 병합한 기존 필드)가 이미 정답이었고, `parseItem()`이 이름에 `[N]`이 없을 때 `base.slots`로 추정하던 기존 폴백과도 그대로 맞아떨어졌다. 대상 558종(장비류만, 소모품·카드는 slots 필드 자체가 없어 무관 확인)
- **작업 중 발견한 더 큰 문제**: 드롭명에 `[N]`이 항상 붙으면, 인벤토리 키를 `parseItem()` 없이 `DB.items[key]`로 직접 조회하던 코드가 전부 조용히 죽는다는 걸 확인. 이전엔 카드 박은 극소수 아이템만 겪던 문제가 이 변경으로 **일반 장비 드롭 절반 가까이**(558/2876종)로 확산될 뻔했다. 착수 전 `grep`으로 전수조사해 8곳 발견 후 같이 교정:
  - 상점 판매탭 필터·렌더(2곳) + `shopSell()` 퀘스트아이템 가드 — 안 고쳤으면 **소켓 있는 드롭템을 NPC에게 하나도 못 팔 뻔함**(판매목록에서 통째로 사라짐, 가장 심각)
  - 노점(vending) 등록 가능 아이템 목록 필터 — 마찬가지로 목록에서 사라짐
  - 창고(카프라) 인벤/보관함 표시 — 아이콘만 📦로 깨짐(기능은 살아있음, 경미)
  - 무게 계산 루프 — 소켓 있는 장비의 무게가 0으로 누락(무게 시스템 과소평가)
  - 크루세이더 스킬 `쉴드 부메랑`·`쉴드 차지` — 소켓 있는 방패 장착 시 방패 DEF 보너스 미반영
  - `calcStats()` 장비 루프·`equipItem()`은 이미 `parseItem()`을 쓰고 있어 무사했음 — 이게 이 프로젝트의 표준 패턴이라는 걸 확인, DEVREF-INDEX에 규칙으로 남김
- 검증: JS `<script>` 블록 9개 전부 `node --check` 통과, node로 `parseItem()` 로직만 떼어내 `"메일 [1]"`·`"메일"`·`"메일 [1] <포링>"` 세 케이스가 전부 같은 `base`/`slots`로 귀결되는지 직접 실행 확인. `build.py` 재실행 diff가 이번 수정분과 정확히 일치(핵심 1곳 + shieldDef 2곳 = 4줄, parseItem 교정 6곳 = 27줄)
- P1-03 완료. DEVREF-E는 완료 포인터로 축약, P1 잔여 건수 6→5
- 오늘 밟은 함정: 백로그에 "규칙이 설계 안 됨"이라 적혀 있었지만 실제론 이미 있는 필드(`slots`)를 그대로 쓰면 되는 문제였다 — 새 필드·새 확률 설계를 먼저 고민하다 코드를 다시 읽고서야 기존 폴백과 어긋나지 않게 맞추면 끝이라는 걸 확인. **"설계 안 됨"이라는 백로그 문구를 곧이곧대로 믿지 말고 관련 코드부터 다시 읽을 것**

-----

## 2026-09-20 (계속) — P1-01: 백로그 원인 재규명 + `procPlayerStatusTick()` 실시간화

- **착수 전 재검증에서 백로그 진단이 틀렸다는 걸 확인**: 문서는 "스킬 쿨다운·화살 hitEffect proc이 ASPD에 비례 가속"이라 적었지만, 코드를 직접 손으로 계산해보니:
  - 스킬 쿨다운: `G.cooldowns[skill] = round(쿨타임ms / aspdDelay)`로 틱수를 정하고, 틱 하나는 실제로 `aspdDelay`ms마다 옴(`scheduleTickAspd()`가 `setTimeout(huntTick, s.aspdDelay)`로 매번 새 aspdDelay를 씀). `틱수 × aspdDelay = 쿨타임ms`로 ASPD가 고정인 한 정확히 상쇄됨 — **버그 아님**, 이미 실시간 정확
  - 화살 hitEffect proc: 공격 1회당 확률 1번 굴리는 구조라 ASPD 높아 공격이 잦아지면 초당 proc 기회도 느는 게 정상 — 원작 RO도 동일. **버그 아님**
- **실제 원인 특정**: `procPlayerStatusTick(p)`(4808행 부근) — 몬스터가 플레이어에게 건 상태이상(독·스턴·동빙 등)의 `turns` 감소가 `processTurn()` 호출마다(=플레이어 자신의 ASPD 틱 주기) 일어남. 몬스터 공격속도는 안 바뀌는데 플레이어가 빨라질수록 **플레이어가 받은 디버프가 실시간으로 더 빨리 풀림** — 이게 "고AGI 캐릭터 사실상 무적"의 진짜 메커니즘. 자기 버프(블레싱 등)도 같은 함수라 반대로 실시간보다 빨리 닳는 부작용이 있었음(플레이어 손해쪽이라 안 보였을 뿐)
- **사용자 확인**: 범위를 "플레이어 전체 statusEffects(디버프+버프 다)"로 확정 — 몬스터/타겟 쪽은 이번 범위 밖
- 한 것: `procPlayerStatusTick()` 맨 앞에 자연회복(`procNaturalRegen`)과 동일한 실시간 게이트 3줄 추가(`p._lastStatusTickAt` 타임스탬프, 1000ms 미만이면 즉시 return). 기존 turns-- 로직·독/생츄어리/맥시마이즈파워 매턴 효과는 그대로 — **함수 전체를 실시간 1초 단위로만 재포장**했을 뿐 개별 effect 로직은 안 건드림(타겟 패치)
- 검증: JS 9블록 `node --check` 통과. 게이트 로직만 노드로 떼어내 시뮬레이션 — 100ms/틱(빠른 ASPD)로 2000ms 동안 20번 호출해도 실제 감소는 1회(1000ms 지점)만 발생, 1200ms/틱(느린 ASPD)은 2400ms 동안 2회(1200·2400ms) — ASPD 무관하게 실제 1초당 1회로 확인. `build.py` diff 9줄 = 추가한 게이트 코드와 일치
- P1-01 완료. DEVREF-E·DEVREF-G 둘 다 갱신(원인 재규명 포함), P1 잔여 건수 5→4
- 오늘 밟은 함정: **백로그 문서의 진단을 검증 없이 믿지 않은 게 맞는 판단이었다.** 손으로 수식을 계산해보지 않았으면 "스킬쿨·화살proc을 실시간화"하는 헛수고를 하면서 진짜 원인(`procPlayerStatusTick`)은 그대로 남겨뒀을 것 — 백로그는 "증상"은 정확히 기록했지만 "원인 지목"은 틀렸던 사례. 증상(고AGI 무적)만 믿고 원인(스킬쿨/화살)은 코드로 재검증할 것

-----

## ⏳ 다음 세션 확인 요청 — v6.0 라우팅 실효성 (2026-08-24 등록, 미결)

문서 개편(v6.0) 검증 중 **유일하게 안 닫힌 항목**이고, 자동화가 불가능해 실작업 세션에서만 확인된다.

- **확인할 것**: 이 프로젝트(비-BIM, App 도메인) 작업을 `BIM\CORE_BIM_DEVREF.md`를 **읽지 않고** 끝낼 수 있는가?
- **읽어야 했다면** 그 법칙은 원래 도메인 무관(Global)이었던 것이다 → `CORE_DEVREF`로 되돌리고 감사에 기록
- **결과 기록처**: `_check-doc\감사_2026-08-16.md` §6 4번
- 확인 끝나면 이 절을 지운다
